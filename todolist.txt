KnowledgeMap Todo
================================
前端部分：
说明：
- 这个文件在每次实现完其中一项内容后都要更新。
- 清单尽量保持短，不展开成大而全的路线图。
- 当前短目标：Dashboard 首页体验优化，把静态统计面板替换为有实际用途的日历组件。

============
2026-06-30
============
[x] 1. 将 Dashboard 首页 hero 区右侧的"收纳项目 / 可用入口 / 界面模式"三栏统计面板替换为日历组件：新增 `calendarYear / calendarMonth / calendarDays` 响应式状态与 `prevMonth / nextMonth` 切换函数；模板改为 `.hero-calendar` 容器，内含月份标题、星期行和日期网格；今日高亮使用 `--primary-btn-border` 强调色 + glow 投影；前后月溢出日以低透明度展示；整体视觉保持与暗色/亮色双主题的 glass-panel 风格一致。
[x] 2. 修正 Dashboard 首页日历视觉：将 `.hero-calendar` 从突兀白底改成贴合当前背景的半透明 glass-panel；统一边框、阴影、hover 和明暗主题变量；今日日期不要再用黑色实心圆遮住数字，改成高对比描边/柔和底色/强调色文字，确保 2026-06-30 的“30”清晰可见。
[x] 3. 增加日历基础交互：点击日期后设置 `selectedDate`，当前月日期可选中，前后月溢出日期点击后切换到对应月份并选中该日；选中态、今日态、有事项标记三种状态要能同时共存且不互相遮挡。
[x] 4. 增加日期详情弹窗：点击某天在屏幕中间打开轻量 modal，显示日期标题、简单 todolist/计划安排/会议示例数据；弹窗支持关闭按钮、点击遮罩关闭、Esc 关闭，移动端不溢出屏幕。
[x] 5. 为后续真实数据预留结构：先用本地 mock 的 `calendarEvents` 映射日期到事项，日历格子用小圆点或细线提示当天有安排；之后可接记账模块或计划模块，把某天账单、待办、会议统一放进弹窗。

============
2026-07-05
============
[x] 6. 为 Dashboard 日历弹窗增加事项“增加”和“删除”功能：`calendarEvents` 改为响应式数据；弹窗内新增时间、类型、标题、说明表单；提交后把事项加入当前日期并同步日历小圆点；每条事项增加删除按钮，删除最后一条时清空该日期标记；补齐移动端弹窗布局并通过前端构建验证。
[x] 7. 将 Dashboard 日历事项接入本地 MySQL `knowledgemap`：新增 `calendar_events` 表和后端 `/api/calendar-events/` 增删查改接口；前端从数据库加载可见日期范围，新增/删除改为调用 API，刷新页面后事项可保留；已验证后端导入建表、API 创建查询删除闭环、前端构建通过。
[x] 8. 补充数据库结构说明与注释：更新 `表结构/knowledgemap.sql`，说明 `tags / bills / calendar_events` 三张表用途和关系，并为三张表及全部字段添加中文 COMMENT；同步将表注释和字段注释写入本地 MySQL `knowledgemap`，已用 `SHOW TABLE STATUS` 和 `SHOW FULL COLUMNS` 验证。
[x] 13. 调整 Dashboard 首屏右侧四宫格比例：收紧日历卡高度、日期格尺寸、日程列表间距和整体 widgets grid 行高，避免日历过大挤压“本周进度”；保留日历数据库交互逻辑，并通过前端构建验证。
[x] 9. 优化前端界面的设计性与动画流畅度（Anti-Slop 视觉升级）：在 `main.css` 中引入 SVG 拓扑流光粒子、账单列表瀑布流渐次级联、按键触感物理反馈关键帧与公共类；在 `Dashboard.vue` 中绑定 `@mousemove` 计算坐标实现项目卡片“鼠标跟随径向发光”，升级日历事项弹窗为 `cubic-bezier` 阻尼弹性果冻过渡，为日历有事项的标记点增加呼吸 Pulse 动效；在 `Notes.vue` 中添加 SVG Dashed Flow Lines 动态连线以模拟数据传输流动，在节点悬停和激活时渲染顺时针旋转的虚线锁定环（`node-lock-ring`），重构 Reader Drawer 滑入滑出为 cubic-bezier 平滑吸附过渡；在 `Bills.vue` 账单列表注入瀑布流延迟样式，为主按钮和弹窗操作配置 `btn-tactile` 反馈并完成前端构建验证。
[x] 10. 重构 Dashboard 背景视觉体验：移除首屏硬边缘的旋转圆圈（`ink-line`）和 `drift` 关键帧动画；挂载全屏 HTML5 `<canvas>` 背景，实现轻量级神经网络星座粒子引擎（Constellation Canvas）。粒子自动漂移并在距离靠近时拉出细微连接线；粒子自动感知鼠标位置并产生引力场靠拢拉出连线；且绘制色彩与当前主题自适应切换（暗色青，亮色紫），完美契合 "KnowledgeMap" 拓扑品牌调性，并顺利通过前端构建。
[x] 11. 净化首屏背景环境：完全移除 Dashboard 英雄背景中规整的网格背景线元素（`.grid-plane` 节点与相关 CSS），消除线条杂音，让星辰粒子神经网络背景更加清透、聚焦。
[x] 12. 全面重构 Dashboard（http://localhost:3000/）首页布局与视觉系统：按照设计蓝图进行高端科技感重塑。导航栏重写为中英文品牌组合，并引入右侧圆形用户渐变头像（K）；首屏升级为分栏，左侧集成 kicker 胶囊与三大玻璃态核心数据指标卡片，右侧重载为双列式（Widgets Columns）交互卡片堆叠（彻底解决 2x2 格局在高度不一时拉伸形变的横纵比失调问题，自适应今天天气与 5 天趋势、日程日历与今日安排、待办清单及交互式进度条、周进度环形图与本周指标）；重构项目总览区（PROJECT INDEX），新增筛选统计过滤栏（支持实时输入搜索、状态过滤、网格/列表视图切换），重塑 6 大高度定制化的项目大卡片（分别包含钱包/网络/大脑/服务器/云/文件图标，特有的详情参数、进度条以及状态标签），右下角配齐多维悬浮 FAB 菜单按钮，并顺利通过前端打包构建。

============
2026-07-06
============
[x] 14. 对照 `UI预览图/dashborad界面1.png` 继续修正 Dashboard 首屏：取消后续区块强制隐藏；调整首屏左右分栏、标题单行显示、右侧四卡上大下小布局；覆盖日历日期格 `aspect-ratio`，修复“今日安排”溢出到“本周进度”的问题；改为可见型入场动画，并用 Chrome headless 截图和前端构建验证。
[x] 15. 排查并修复 `/bills` 读不到本地 MySQL 数据的问题：确认 8000 端口实际运行的是 `yb_reconcile_demo` 服务，导致 Vite 代理命中错误后端；将 KnowledgeMap 后端默认端口调整为 8010，Vite 代理默认指向 8010 且支持 `KM_API_TARGET` 覆盖；账单页进入时自动切换到数据库最新账单月份，并显示接口错误提示；已验证 `/api/bills` 返回 29 条 2026 年 6 月账单。
[x] 16. 接入和风天气天河区实时数据并完善 Dashboard 天气卡片：后端通过 `/api/weather/` 代理实时天气、7 日预报及 AQI，凭据从 `backend/.env` 读取并使用 30 分钟缓存；天气卡片补充湿度、风况、降水、更新时间和数据来源，同时以低对比度环境光填补留白，保持现有深色仪表盘层级；已通过 Python 编译和前端构建验证。
