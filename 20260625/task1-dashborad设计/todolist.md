# Task 1 Dashboard 设计与主页开发 — 开发记录与清单

> 状态：已完成
> 目标：开发具有高度视觉美感、流畅交互和暗/亮主题切换的项目门户主页。

---

## Phase 1: 视觉风格设计与系统初始化

- [x] 1.1 分析并提取 `frontend_example` 中的静态视觉要素
      → 重点：沉淀卡片发光边框、网格背景、扫光过渡等样式规范。
      → 调色盘：主色为深紫 `#7c3aed`，辅助色为蓝绿 `#06b6d4`。
- [x] 1.2 重构主页骨架结构 (`Dashboard.vue`)
      → 分离组件区域：Hero 头部看板、模块索引网格、文章画廊、关于作者面板。
      → 确认所有元素都包裹在响应式弹性容器中。
- [x] 1.3 接入平滑滚动与滚入动画依赖
      → 引入 Lenis.js 以消除滚动顿挫。
      → 编写 Vue Composables 并通过 Intersection Observer 动态赋予元素滚入淡出效果。

## Phase 2: 页面内容布局与精细排版

- [x] 2.1 编排模块卡片网格布局
      → 记账（Bills）和 KnowledgeMap 状态设定为“可进入”。
      → AutoML 等后续子系统卡片置灰，提示“预留/等待接入”且不可点击。
- [x] 2.2 实现 Journal 三栏排版
      → 分类标签、大标题、摘要内容布局，三栏需实现响应式等高展示。
      → 调整“《星光咖啡馆与死神之蝶》”栏目标题字号，缩小一号以保证行高和结构整齐。
- [x] 2.3 设计 Newsletter 邮箱订阅条
      → 实现带输入过滤的前端 Form 表单。
      → 鼠标悬停订阅按钮时，加入向右滑动的背景填充动画。

## Phase 3: 静态资源引用与自适应裁剪

- [x] 3.1 引入三栏卡片大图
      → 卡片依次引用 `/images/pic1.png`、`/images/pic2.png`、`/images/pic3.png`。
- [x] 3.2 引入作者元数据动态头像
      → 在张斯涵（小字文章元信息）旁挂载 `/images/pic4.gif`。
      → 保持头像尺寸极小（如 20px * 20px 圆形），不干扰排版。
- [x] 3.3 实现 About 版块作者大图自适应
      → 采用容器背景图形式：`background: url('/images/pic5.jpg') no-repeat center center;`。
      → 设定 `background-size: cover`。在视口尺寸改变导致宽高比与原图不一致时，自动拉伸裁剪并填满占位，防止出现白边或拉伸变形。

## Phase 4: 全局 CSS 变量重构与 Light / Dark 双主题

- [x] 4.1 提取核心视觉属性为 CSS 变量
      → 将背景色、卡片色、文字主次色、边框色提取到 `styles/main.css`。
- [x] 4.2 编写 `theme-light` 样式覆盖集
      → 配色以温润米白 `#fcfbfa` 和卡片白 `#f4f2ee` 构成，保留原深色模式卡片的柔和外发光质感。
- [x] 4.3 实现 Header 顶部模式切换按钮
      → 使用 `localStorage` 自动记忆用户对亮/暗模式的选择。
      → 切换时流畅过渡文字与背景色阴影，防止切换瞬时闪屏。
