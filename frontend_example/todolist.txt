TP Todo
================================
脚本部分：
说明：
- 这个文件在每次实现完其中一项内容后都要更新。
- 清单尽量保持短，不展开成大而全的路线图。
- 当前短目标：公费 + 医保混合外账先稳定收口，然后开始做第三方核验增强，整体逻辑继续复用“门诊 + 住院”的对账框架。
- 医保阶段的实现边界：不动 `yb_reconcile_total.py`，只在 `gf_reconcile_v1.py` 补医保部分。
- 第三方阶段的实现边界：仍然不动 `yb_reconcile_total.py`，继续在 `gf_reconcile_v1.py` 上做；先做“观察增强/核验增强”，不直接升成最终硬判定。

[x] 1. 给 `gf_reconcile_v1.py` 增加 `zhu_yuan_tian_shu` 判断，先把门诊 / 住院分流。
[x] 2. 收紧 `gf_reconcile_v1.py` 的输出口径：门诊继续按当前逻辑对账，住院和门诊+住院混合账先单独标记，不直接算失败。
[x] 3. 接入住院内帐表：先用 `IP_SETTLEBILL_MASTER` 为主、`IP_INPATIENT_REGISTER` 为辅，打通住院取数。
[ ] 4. 住院匹配优先按 `FOPERATION_NO` / `FREGISTER_NO`，其次 `FIP_SERIALNO`，最后才退到姓名 + 时间。
[x] 5. 复查公费剩余异常样本并收口演示口径：最后 1 人保留为“2026-02-08 有 12 元门诊自费记录，2 月 11 日 ~ 2 月 12 日 演示时口头说明，不在本轮强行处理”。
[x] 6. 在 `gf_reconcile_v1.py` 中增加“医保门诊按姓名匹配”的入口设计，明确不改 `yb_reconcile_total.py`。
[x] 7. 复用公费对账框架做医保版本：先明确医保外账表、日期口径、总额/记账字段映射。
[x] 8. 医保门诊先接内账 `OP_SUBINVOICE_2026`，保持与公费一致的姓名 + 日期窗口匹配，不动现有第三方对账逻辑。
[ ] 9. 医保住院继续复用 `IP_SETTLEBILL_MASTER` + `IP_INPATIENT_REGISTER`；当前先沿用姓名 + 时间窗口，结算号/登记号/记帐号优先级后续补。
[ ] 10. 输出医保对账首版结果，并单独标记“记账对齐但总账差额待解释”的样本。
[x] 11. 排查并修复外账重复来源问题：确认曾有一个数据库被复制导致同名同日外账重复，当前问题已定位并处理。
[ ] 12. 重新跑一版 `gf_reconcile_v1.py`，确认公费 + 医保混合外账在去重后结果稳定。
[ ] 13. 开始第三方核验增强：先只观察内账自费部分，不把第三方结果纳入最终成功/失败判定。
[ ] 14. 第三方核验先聚焦门诊内账 `OP_SUBINVOICE_2026`，继续复用 `SUM_PAYMENT` / `PAYMETH_DESC` / 时间窗口。
[ ] 15. 输出第三方观察首版结果：区分“命中 / 未命中 / 无需检查 / 疑似重复或歧义”。
[x] 16. 在 brief 中单列特殊剩余内账：纯 0 金额记录，以及同一人可正负相消的剩余 0 记账记录。
[x] 17. 第三方对账需求变更：将“内账-医保/公费主对账”和“内账-自费/自付对账”解耦，不再要求外账先命中内账后才进入第三方观察。
[x] 18. 医保外账第三方候选字段：`ge_ren_zhi_fu_zong_jin_e`、`ge_ren_zi_fu_fei_yong`、`ge_ren_zhi_fu`、`zi_fei_jin_e`；仅识别到字段的表参与第三方对账。
[x] 19. 公费外账第三方候选字段：`ge_ren_zhi_fu_zong_jin_e`、`zi_fu_jin_e`；仅识别到字段的表参与第三方对账。
[x] 20. 第三方首步实现边界：若外账表未命中上述字段，则该表当前不参与第三方对账。
[x] 21. 输出个人支付观察 CSV：同一行同时呈现主对账结果、外账自付候选、内账个人支付金额、差值与 PAYMETH_DESC 参考。
[x] 22. 新增 `gf_reconcile_v2.py`：仅做门诊、仅做姓名同天双方唯一的一对一匹配，输出 `gf_reconcile_v2.csv`，并追加第三方 1 小时内金额相同的命中信息。
[x] 23. 扩展 `gf_reconcile_v2.py`：新增“多对一”输出，条件为内账同名同天数量 >= 2、外账同名同天数量 = 1；并排除已在一对一中命中的内外账。
[x] 24. 新增 `gf_reconcile_v3.py`：单独做“第三方外账 -> 门诊 PAYMETH_DESC 支付项”匹配，按金额相同且 3600 秒内命中，输出匹配成功与未命中的第三方外账到 `gf_reconcile_v3.csv`。
[x] 25. 调整 `gf_reconcile_v3.py` 候选口径：`PAYMETH_DESC` 中拆出的所有金额项都可作为候选；若同一笔外账金额命中多个候选，按与 `LOGLADATE` 时间差最小优先，并输出秒差。
[x] 26. 收紧 `gf_reconcile_v3.csv` 的第三方列：只保留第三方时间、第三方金额、第三方来源名称，其余第三方明细列不再输出。
[x] 27. 收紧 `gf_reconcile_v3.csv` 列：只保留状态、第三方来源名称、第三方时间、第三方金额、内账时间、内账支付方式、内账SUM_PAYMENT、PAYMETH_DESC、时间差秒数；终端补打印拆分前的 subinvoice 条数。
[x] 28. `gf_reconcile_v3.py` 终端补打印 `PAYMETH_DESC` 拆分分布：多少条 subinvoice 被拆成 1 项、2 项、3 项等。
[x] 29. `gf_reconcile_v3.py` 终端补打印最终结果里命中的 subinvoice 去重条数。
[x] 30. `gf_reconcile_v3.py` 新增 `gf_reconcile_v3.txt`，列出未命中的内账支付项。
[x] 31. `gf_reconcile_v3.py` 为 `gf_reconcile_v3.txt` 增加开关：`ENABLE_UNMATCHED_INNER_TXT`，关闭后跳过 TXT 生成。
[x] 39. `gf_reconcile_v3.py` 调整 `PAYMETH_DESC` 金额项保留口径：只排除 `PAYMETH_DESC` 为空的记录；对非空 `PAYMETH_DESC`，正数、负数、0 都保留参与拆分统计与支付项生成，不再只保留正数。
[x] 40. `gf_reconcile_v3.py` 针对支付宝表 `t_21228zfbsj_zwzhcx1` 增加表内正负冲销预处理：按 `zhi_fu_bao_jiao_yi_hao` 先用退款记录回找同金额的正向支付记录，一收一退先内部冲销后再进入第三方匹配。
[x] 41. `gf_reconcile_v3.py` 的 CSV 追加中行 POS 表 `zgyhylposjsj_sheet1` 的 `zhi_fu_lei_xing` 列；仅该表回填，其他第三方表留空。
[x] 46. `gf_reconcile_v3.py` 调整 `PAYMETH_DESC` 支付项口径：正数和负数保留，金额为 0 的支付项自动过滤，不参与匹配、不进入 `gf_reconcile_v3.txt`，并在终端打印过滤数量。
[x] 47. `gf_reconcile_v3.txt` 的未命中输出继续按支付项逐条展开；若同一笔内账有两个未命中的 DESC 片段，则输出两条，且每条只展示对应的 DESC 片段而不是整条 `PAYMETH_DESC`。
[x] 32. `yb_reconcile_demo` 起步：补应用配置 `app/core/config.py`、业务配置 `app/core/reconcile_config.py`，并新增健康检查接口 `/api/health`。

===========================================================
项目部分：
[x] 33. `yb_reconcile_demo` 先把库表结构的配置写好。
[x] 34. `yb_reconcile_demo` 后端新增 1 个健康检查接口。
[x] 35. `yb_reconcile_demo` 后端新增 1 个对账任务提交接口。
[x] 36. `yb_reconcile_demo` 前端新增 1 个最小页面壳子。
[x] 37. `yb_reconcile_demo` 去掉对 `yb_reconcile_total.py`、`gf_reconcile_v1.py` 等旧脚本的直接依赖；将 demo 需要的默认日期、数据库连接、第三方来源配置、外账候选字段规则迁移到项目内部。
[x] 38. `yb_reconcile_demo` 健康检查接口增强：在返回配置摘要之外，对 `his_inner`、`lyt_2_yue`、`lyt_26_2_gf`、`lyt_26_2_yb_2` 做轻量数据库连通性检查（`SELECT 1`），并返回逐库状态与错误信息。
[x] 42. `yb_reconcile_demo` 规划“记账对账”接口请求体：首版只接收 `start_date`、`end_date`，先只做“记账”主线，不接第三方、不接个人支付。
[x] 43. `yb_reconcile_demo` 规划“记账对账”接口的外账 5 类结果：1) 记账和总账都对上；    2) 记账对上，总账差额可解释；3) 记账对上，总账差额不可解释；4) 记账对不上但有候选；5) 无候选。以上 5 类都以外账视角输出，且应覆盖全部外账，并显式区分门诊 / 住院。
[x] 44. `yb_reconcile_demo` 规划“记账对账”接口的剩余内账 2 类结果：6) 外账成功吸收后的剩余内账里，门诊按 `SUM_TOTAL` 同名正负相消、住院按 `FSUM_TOTAL` 同名正负相消；7) 最终所有未被使用的内账。以上两类都要显式区分门诊 / 住院。
[x] 45. `yb_reconcile_demo` 规划“记账对账”接口的总口径：先完成外账 1~5 类分类，再从未被外账使用的内账中继续归到 6~7 类，确保这段时间内的外账和内账都能被整体解释。
[x] 48. `yb_reconcile_demo` 调整“记账对账”接口匹配策略：同日与前后 1 天结果都要计算，最终按全局更优解选结果；跨天“精确对齐”应优先于同日“13/35 可解释差额”。
[x] 49. `yb_reconcile_demo` 细分“最终未使用的内账”：将其中“记账=0 且 总账=0”的记录单独抽为第 8 类，其余记录继续保留在第 7 类。
[x] 50. `yb_reconcile_demo` 前端“记账对账”页面同步适配第 8 类：摘要卡片增加 `category_8_count`，分类明细增加“记账和总账都为 0 的未使用内账”面板。
[x] 51. `yb_reconcile_demo` 前端“记账对账”页面增加状态记忆：切到其他页面再切回时，表单、健康检查结果和对账结果通过 `sessionStorage` 自动恢复。
[x] 52. `yb_reconcile_demo` 将”记账对账”页面改成”8 类各自分页”的版本；后端分页接口已完成（`page`/`page_size` 参数，每个 category 返回 `items` + `pagination`），前端已适配新响应结构并为每个分类添加 `el-pagination` 控件。
[x] 53. `yb_reconcile_demo` 在“记账对账”页面先补数据源筛选壳子：放在时间筛选下方，做成默认折叠、默认不启用的分层筛选器；第一层分“公费 / 医保”，第二层预置各自细分项，当前先不接后端筛选逻辑。
[x] 54. `yb_reconcile_demo` 前端适配后端分页接口：从 `categories[key].items` 取数据、`categories[key].pagination` 取分页信息，接入 `el-pagination` 分页控件，每个分类独立翻页，翻页状态通过 `sessionStorage` 持久化。
[ ] 55. `yb_reconcile_demo` 新增“结果统计 / 数据分布”界面：先搭页面壳子和图表占位区，具体图表类型与指标后续再细化。
[x] 56. `yb_reconcile_demo` 将前端已有筛选界面接到应用逻辑：首版已接通时间范围 + 数据源分层筛选到“记账对账”接口；后端按前端勾选的叶子表收窄外账表范围，同时继续应用 `outer_row_filter_config.py` 的表内排除规则，结果列表与分页随筛选联动更新。
[x] 57. `yb_reconcile_demo` 在“记账对账”页面为外账 1~5 类结果增加可展开明细：一级表格继续展示摘要，展开后明确展示“哪一笔外账对应了哪一笔或哪些笔内账”，先按之前 `brief/txt` 的对账成功明细风格落地。
[x] 58. `yb_reconcile_demo` 在“记账对账”页面为第 6 类“同名正负相消（剩余内账）”补可展开明细：展开后分别展示正向内账和负向内账，明确哪些账之间互相冲销。
[x] 59. `yb_reconcile_demo` 将外账“脏数据/重复来源”排除规则独立成单独配置文件维护；首条规则为：医保表 `sbb_kszy` 中 `ren_yuan_lei_bie = 职工` 的记录全部忽略，不参与匹配。
[x] 60. `yb_reconcile_demo` 为“结果统计 / 数据分布”先补路由和页面壳子：页面先包含筛选条件回显区、汇总卡片区、图表区、明细区四块。
[x] 61. `yb_reconcile_demo` 梳理统计页首批指标：首版已明确复用 `summary`，并由后端补充“外账分类分布 / 内账剩余分布 / 门诊住院分布 / 外账来源分布 / 外账表分布”等聚合数据。
[x] 62. `yb_reconcile_demo` 为统计页准备首版数据结构：已返回“分类数量分布”“门诊/住院分布”“外账来源分布”“外账表分布”等统计结果，并可随记账结果同步更新。
[x] 63. `yb_reconcile_demo` 为统计页落首版展示组件：已完成汇总卡片、外账分类分布、来源/就诊类型分布、剩余内账分布、外账表 Top 10 等首版展示，图表库与视觉样式后续再细化。
[x] 64. `yb_reconcile_demo` 规划“自付对账”首版主线：已在项目中补出 `/api/reconcile/self-pay` 骨架与前端页面确认流程，明确以 `gf_reconcile_v3.py` 为参考，不走“外账自付候选 vs SUM_PAYMENT”主线，而是走“第三方外账 -> 门诊 `PAYMETH_DESC` 拆分支付项”的匹配思路。
[x] 65. `yb_reconcile_demo` 修正“自付对账”第三方口径：第三方外账进入匹配前不再先做表内正负冲销，必须保留冲销前的原始正负流水逐笔参与对账，避免掩盖“外账漏正账 / 漏负账”的责任归属。
[x] 66. `yb_reconcile_demo` 规划“自付对账”门诊匹配口径：只做门诊，先从 `OP_SUBINVOICE_2026` 的 `PAYMETH_DESC` 中拆出支付项；第三方外账按“时间 + DESC 金额”匹配，不以人名作为匹配条件，人名仅在结果展示阶段回填。
[x] 67. `yb_reconcile_demo` 规划“自付对账”首版结果结构：整体口径参考 `gf_reconcile_v3.py`，至少要能区分“第三方命中”“第三方未命中”“未命中的内账支付项”，并支持分页返回。
[ ] 68. `yb_reconcile_demo` 规划“自付对账”范围边界：住院部分本轮暂不实现，后续单独设计；当前先把门诊第三方对账链路跑通。
[x] 69. `yb_reconcile_demo` 规划“自付对账”参考输出：接口返回之外，首版还要在后端项目目录额外写出一份 `txt` 结果文件，重点保留未命中的第三方外账和未命中的内账支付项，供后续继续加约束条件时人工分析。
[x] 73. 新增独立脚本：遍历 `lyt_2_yue` 全部第三方库表，按 [问题.txt](c:/Users/afrangry/PycharmProjects/tp/问题.txt) 里约定的规则整理各表关键字段与规整化结果，已输出全量底稿 CSV/TXT，并额外输出精简版 CSV，帮助确认“来源名称 / 时间 / 金额 / 支付方式 / 卡号 / 卡种 / 发卡机构 / 交易类型”等口径。
[x] 74. 将上述第三方遍历脚本中的“规整化能力”从一次性脚本里抽出来，沉淀成可复用的标准化加载逻辑；后续“自付对账”的第三方外账基础数据统一从这套规整化结果生成，而不是直接拼原始第三方表。
[ ] 75. 在第三方规整化脚本与后续复用逻辑中，统一保留冲销前的原始正负流水、原始金额字段名、规整后方向/符号、来源表名与关键原始字段，确保后续详细 CSV 能支持逐笔排查。
[x] 76. 新增独立脚本：基于 `his_inner.lyx_mz_zifu_detail` 与项目内第三方标准化结果，按“时间 + 金额 + 支付方式映射”先跑通门诊自付对照；映射关系来自 `yb_reconcile_demo/scripts/内外帐对照映射.txt`，输出详细 CSV/TXT 供后续再迁入接口。
[x] 77. 优化 `yb_reconcile_demo/scripts/script3.py`：参考第一个接口，已先补并行取数（内账拆分表 + 第三方标准化结果）以及“按来源表 + 金额”索引后的匹配，避免每条内账都全表扫描第三方结果。
[x] 78. 继续优化 `yb_reconcile_demo/scripts/script3.py`：第三方标准化加载支持“只加载映射中实际会用到的表”并发取数，内账只查询映射中实际会用到的 `DESC_TYPE`，并在“表 + 金额”桶内按时间窗口二分切片，进一步缩短脚本等待时间。
[x] 79. 收紧 `yb_reconcile_demo/scripts/script3.py` 的 CSV 输出：对齐 `gf_reconcile_v3.csv` 的阅读风格，仅保留“状态、姓名、第三方来源名称、第三方金额、第三方支付方式、内账拆分金额、内账支付方式、第三方时间、内账时间、时间差秒数、内账SUM_PAYMENT、PAYMETH_DESC、匹配规则、子发票号”等核心列，并按约定顺序输出。
[x] 80. 收紧 `yb_reconcile_demo/scripts/script3.py` 的映射口径：当前只保留 `内外帐对照映射.txt` 中“优先级 = 1”的映射规则，所有优先级为 2 的映射方式先不参与匹配。
[x] 81. 将 `yb_reconcile_demo/scripts/script3.py` 的单一结果 CSV 拆成三份：匹配成功结果保留当前简洁格式；内账未命中结果按 `lyx_mz_zifu_detail` 表结构字段输出；外账未命中结果按 `third_party_normalized_all_brief.csv` 的精简字段输出。
[x] 82. 将 `yb_reconcile_demo/scripts/script3.py` 的核心逻辑迁入项目：新增 `/api/reconcile/self-pay` 的真实实现，首版返回“第三方命中 / 内账未命中 / 第三方未命中”三类分页结果，并在项目 `outputs/self_pay/` 下同步写出参考 CSV/TXT；前端“自付对账”页面同步接入执行与结果展示。
[x] 83. 将“记账对账”页面的结果展示按就诊类型拆开：后端在原总分类结果之外，额外返回“门诊 / 住院”两套分类结果；前端改成两个标签切换，分别展示门诊对账结果和住院对账结果，避免同一人门诊与住院记录混排。
[ ] 84. 记录“记账对账”当前已知边界：同一笔账横跨 6 天是可能的（如提前多日线上预约挂号），当前程序主要按同日 / 前后 1 天窗口匹配，这类跨多日样本可能无法被很好对齐，后续需单独收口规则。
[ ] 85. 后续几轮迭代后，为“记账对账”引入“颜色账”口径：当前按姓名匹配在“同一人一天只有一笔外账”时风险较低；若同名者同一天存在两笔外账，则需要结合身份证判断是同一人还是两个同名的人。医保外账有身份证号、公费外账没有身份证号，因此后续即便金额能对齐，也要区分“可直接认定成功”与“仍需人工复核”的颜色层级。
[ ] 86. 为“自付对账”设计颜色分层：严格按 `内外帐对照映射.txt` 的映射方案命中的结果记为绿色账；对剩余内外账再做一轮“不按映射表”的宽松匹配，若数值能对上则记为橙色账（需人工二次检查）；最终仍未对上的记为红色账。
[ ] 87. 为未对上的第三方自付结果准备“结果统计”界面：先聚焦展示未匹配数据的来源分布、支付方式分布、金额分布、时间差分布等，可先用直方图/分布图等基础图形占位。
[ ] 88. 自付对账的绿色结果后续可能不再继续写入 `yb_reconcile_demo/scripts/script3_匹配成功.csv`，而改为按同样字段格式写入数据库表 `lyt_mz_zifu_success`；橙色结果也要预留建表思路，但当前尚未进入 discuss 阶段。
[x] 89. 明确“自付对账”首层落库设计（门诊）：每次执行接口时，先清空并重写中间状态表，不做更新也不做追加；绿色结果分别写入 `lyt_mz_zifu_success`（格式同 `yb_reconcile_demo/scripts/script3_匹配成功.csv`）、`lyt_mz_zifu_fail_his`（格式同 `yb_reconcile_demo/scripts/script3_内帐未匹配.csv`）、`lyt_mz_zifu_fail_dsf`（格式同 `yb_reconcile_demo/scripts/script3_外账未匹配.csv`）。
[x] 90. “自付对账”住院绿色层已挂到同一个 `/api/reconcile/self-pay` 后端链路：沿用与门诊一致的三张表结构，仅将前缀切换为住院；即 `lyt_zy_zifu_success`、`lyt_zy_zifu_fail_his`、`lyt_zy_zifu_fail_dsf`。当前接口执行时会先跑门诊绿色，再用门诊剩余第三方去匹配住院预交金，并把住院绿色结果清空重写到这三张表中。
[x] 91. “自付对账”第二层黄色账流程已挂到同一个 `/api/reconcile/self-pay` 后端链路：将 `lyt_zy_zifu_fail_dsf` 与 `lyt_mz_zifu_fail_his` 再做一轮无映射约束的宽松匹配，仅按“时间 + 金额”匹配，时间限制 `120` 秒；命中结果清空重写到 `lyt_mix_zifu_success`（黄色账，表示金额与时间能对上，但可能把微信/支付宝等错误映射对进来），剩余内账写入 `lyt_mix_zifu_fail_his`，剩余第三方写入 `lyt_mix_zifu_fail_dsf`，并同步输出参考 CSV/TXT。
[x] 92. “自付对账”第三层紫色账流程已挂到同一个 `/api/reconcile/self-pay` 后端链路：对 `lyt_mix_zifu_fail_his` 与 `lyt_mix_zifu_fail_dsf` 中的特殊金额（当前先按 `13`、`21` 两档）单独沉淀；内账特殊金额清空重写到 `lyt_teshu_success_his`，第三方特殊金额清空重写到 `lyt_teshu_success_dsf`，并同步输出参考 CSV/TXT。这一层标记为紫色，表示“内外账中存在很多特殊金额，尚不清楚未成功匹配的原因，可能存在未知计算条件”。
[x] 93. “自付对账”最终红色账已接入同一个 `/api/reconcile/self-pay` 后端链路：在绿色 / 黄色 / 紫色几层筛完后，最终仍未被选中的第三方外账清空重写到 `lyt_teshu_fail_dsf`，最终仍未被选中的内账清空重写到 `lyt_teshu_fail_his`，并同步输出最终失败参考 CSV/TXT。
[x] 94. 将“门诊 - 绿色账”落库拆成两步实现：先独立提供建表 SQL（当前已新增 `yb_reconcile_demo/sql/create_self_pay_outpatient_green_tables.sql`，用于创建 `lyt_mz_zifu_success`、`lyt_mz_zifu_fail_his`、`lyt_mz_zifu_fail_dsf` 三张表）。
[x] 95. 将“门诊 - 绿色账”接口落库实现为“清空并重写”：`/api/reconcile/self-pay` 在门诊绿色对账跑完后，当前已通过独立落库模块清空并批量写入 `lyt_mz_zifu_success`、`lyt_mz_zifu_fail_his`、`lyt_mz_zifu_fail_dsf` 三张表；若表不存在，则提示先执行建表 SQL。
[x] 96. 修正“自付对账”结果库职责：`lyt_2_yue` 仅保存第三方原始外账，`his_inner` 同时保存内账与程序运行中间结果；因此门诊绿色层的 `lyt_mz_zifu_success / lyt_mz_zifu_fail_his / lyt_mz_zifu_fail_dsf` 已改为统一落在 `his_inner`。
[x] 97. 接入住院 - 第三方脚本前，先收口脚本改造点：结果表仍落 `his_inner`；输入不再强依赖人工导出的本地 CSV；“删表重建”改为“独立建表 + 每次清空并重写”；时间阈值注释、提示语与实际值保持一致；连接配置与项目统一，不再在脚本内写死一套账号密码。（当前已完成：统一接项目 `his_inner` 连接、拆出独立建表 SQL、改成“确保表存在 + 清空并写入”、统一 120 秒口径，并将输入从本地 `script3_外账未匹配.csv` 改为直接读取 `his_inner.lyt_mz_zifu_fail_dsf`。）
[x] 98. 修正“自付对账”门诊绿色层的数据保留口径：`lyx_mz_zifu_detail` 中不在映射表里的 `DESC_TYPE`（如“现金”）不再在入口查询时被过滤掉，而是统一纳入本轮数据集；绿色层匹配不上时应进入“未匹配”链路，继续流转到后续黄色 / 紫色 / 红色层，而不是被静默丢弃。
[x] 99. 为 `/api/reconcile/self-pay` 增加后端终端进度输出：在门诊绿色、住院绿色、黄色、紫色、红色各层开始/结束及落库时打印阶段信息、输入输出条数和目标表名，便于接口长时间执行时观察当前进度与卡点。
[x] 100. 将 `/api/reconcile/self-pay` 在 `outputs/self_pay/` 下生成的参考文件名统一改成与中间结果表一致：门诊绿色用 `lyt_mz_*`，住院绿色用 `lyt_zy_*`，黄色层用 `lyt_mix_*`，紫色层用 `lyt_teshu_success_*`，红色层用 `lyt_teshu_fail_*`，便于人工核对“文件输出”和“数据库中间表”。
[x] 101. 同步修正 `yb_reconcile_demo/scripts/script3.py` 的数据保留口径：不再只读取映射表内的 `DESC_TYPE`，而是让所有非空 `DESC_TYPE` 都进入门诊自付对照脚本；映射表之外的支付方式匹配不上时进入“内账未命中”，不再被脚本入口过滤掉。
[x] 102. 为 `/api/reconcile/self-pay` 的住院绿色层与黄色层增加按金额桶的并发匹配：保持“同金额桶内最近时间一对一”口径不变，在不同金额桶之间并发处理，进一步缩短接口等待时间。
[x] 103. 收紧 `yb_reconcile_demo` 的 git 忽略规则：`outputs/` 下的接口参考文件不进仓库，`yb_reconcile_demo/scripts/` 下的生成型大文件（如 `csv/xlsx/txt` 结果文件）也不进仓库，但保留脚本源码与手工维护的映射说明文件。
[x] 70. `yb_reconcile_demo` 调整“记账对账”接口口径：已取消第 6 类“剩余内账中同名正负相消”，不再将内账正负记录先内部解释或冲销后再参与责任判断。
[x] 71. `yb_reconcile_demo` 调整“记账对账”接口匹配边界：已改为门诊外账只和门诊内账匹配，住院外账只和住院内账匹配，不再允许门诊/住院混用候选池。
[x] 72. `yb_reconcile_demo` 调整“记账对账”接口组合策略：已取消固定 `3` 笔上限，改为在“同名 + 时间窗口 + 就诊类型”形成的候选池内做全量求解，并通过剪枝避免组合爆炸。
[x] 118. `yb_reconcile_demo` 前端呈现：“自付对账”界面重构成独立的 5 个面板（横向标签），用来展示从门诊绿色到红色死亡最终未命中的 5 层漏斗结构。
[x] 119. `yb_reconcile_demo` 前端呈现：在自付对账执行界面中同步呈现每层结果的摘要信息，将执行参数选取与统计明细放到一起。
[x] 120. `yb_reconcile_demo` 数据完整性修复：修改内账查询接口 `_load_inner_self_pay_details`，取消 `DESC_TYPE IS NOT NULL` 的限制，确保缺失支付方式的账单能进入对账漏斗进行追踪，而不是被静默漏除。
[x] 121. `yb_reconcile_demo` 黄色容错层规则增强：针对 `DESC_TYPE IS NULL` 的内账数据，如果在黄色层参与兜底容错，增加前置安全校验：仅当该笔内账的 `SUM_TOTAL == SUM_PAYMENT`（即发票全额自费）时，才允许其凭借“金额同+时间近”的特征参与外账重组，否则直接拒绝匹配保护数据安全。
[x] 122. `yb_reconcile_demo` 新增内部冲销层（第三方外账）：在黄层容错匹配之后、紫层特殊金额之前，对剩余的外账进行内部冲销；寻找同来源（`source_name_display` 相同）且卡号相同（若有）的极值相反数（+x 与 -x）对，将其内部平账销账，避免污染后续红层。
[x] 123. `yb_reconcile_demo` 新增内部冲销层（双线内账）：对剩余的门诊内账，寻找同姓名且金额互为相反数的记录对其进行单边冲销；对剩余的住院内账，寻找金额互为相反数且发票号（`FINVOICE_ID`）一致的记录对其进行单边冲销。
[x] 124. `yb_reconcile_demo` “蓝色单边自净层”前后端贯通支持：后端返回成组互相抵消的正负成对明细，提供分页支持；前端新增独立呈现 Tab 面板与顶部数据看板，并在 `el-steps` 动态展示进度。
[x] 125. `yb_reconcile_demo` 底层自付明细缺失符号问题修复：已运行重构脚本 `script5.py` 对二十六万余条底层带歧义金额（如单独的26或.59）从强源头 `SUM_PAYMENT` 获取正确负值覆盖并回写，解决漏斗首段丢负号异常。
[x] 126. `yb_reconcile_demo` 精简用户呈现视图：移除紫色特殊约束层与红色终极死账层的冗余后端文件下载（CSV/TXT）暴露信息，完全切换为可直接网页内动态伸缩及分页查询的独立动态组件表格。
[x] 127. `yb_reconcile_demo` 扩展防腐紫层的特异拦截池：在原本的 13 21 基础上加入更严格的约束控制列表（含 `-13` / `-21` 与正负 `26` 及 `35` 金额），避免杂费引起漏斗错乱。
[x] 128. `yb_reconcile_demo` 将严格对账（原绿色层）延展为三重时间梯度漏斗：新建“门诊-30分、住院-30分”及“门诊-25小时、住院-25小时”四个匹配过滤层；外账流层层跌落递传，门诊内账和住院内账在各自的时间梯度流道平滑前行，彻底剥离并收紧大跨度时间的虚假匹配。
[x] 129. `yb_reconcile_demo` 追加新层级的后市场落位（后端）：新增 4 组匹配函数，新增 12 个中间状态数据表与 12 份对应拆出的匹配情况追溯文件（CSV/TXT），保证数据透明并供审核。
[x] 130. `yb_reconcile_demo` 升级新流水线呈现大盘（前端）：在 `SelfPayReconcile.vue` 中扩建 4 个独立 Tab 仪表盘，让 7 步 `el-steps` 升级为一个恢宏庞大且连贯的长流程执行看板。
[x] 131. `yb_reconcile_demo` 落库逻辑修复：修正了“自付对账”在把外账输出给住院落库系统时发生的 `KeyError: lai_yuan_ming_cheng`，通过提供拼音映射函数 `_map_inpatient_outer` 拦截重构，让发往外账 SQL 的字典字段精准命中列名。
[x] 132. `yb_reconcile_demo` 前端路由与界面扩展：“自付对账”升级为和“记账对账”平齐的树状子路由。新增 `SelfPayStatistics.vue` 空壳大盘占位；抽离原本的执行流页为 `/self-pay/run`，并将 `App.vue` 中的单项菜单拓展为多级 `el-sub-menu`。
[x] 133. `yb_reconcile_demo` 阶段进度条优化纠偏：纠正前端 `el-steps` 将“蓝色自净”提在“黄色层”之前的物理视觉顺序错误；同步在后端拦截拦截“蓝单边1”信号，避免内部蓝色冲销让全局黄色进度条乱跳。
[x] 134. `yb_reconcile_demo` 规划“记账对账”漏斗化改造总方案：参考“自付对账”的多层流水线，将当前 1~7 类分类结果重构为按“规则强度 / 置信度 / 是否需人工复核”逐层跌落的漏斗结构，但在正式改造前先冻结每一层的业务含义与命名。
[x] 135. `yb_reconcile_demo` 规划“记账对账”绿色层：将“记账和总账都对上”以及“高置信、可直接认定成功”的样本沉淀为绿色层，要求门诊 / 住院继续严格分流，并保留逐笔外账对应哪些内账的可追溯明细。
[x] 136. `yb_reconcile_demo` 规划“记账对账”黄色 / 橙色层：把“记账对上但总账差额可解释”“跨天但解释链较弱”“同名多外账下可能存在同名不同人风险”的样本单独沉淀为需人工复核层，后续结合医保身份证、公费缺身份证等差异进一步拆颜色与风险等级。
[x] 137. `yb_reconcile_demo` 规划“记账对账”紫色特殊层：单独收纳预约挂号跨多日、特殊小额、可疑重复来源、以及其它当前规则无法直接归入绿色/黄色但又具备明显业务特征的样本，作为后续专项规则收口池。
[x] 138. `yb_reconcile_demo` 规划“记账对账”红色最终失败层与前端呈现：将最终仍未被解释的外账 / 内账沉淀为红色层，并把“记账对账”执行页逐步改造成和“自付对账”一致的漏斗看板、阶段进度条与分页结果视图。
[x] 139. `yb_reconcile_demo` 固定“记账对账”漏斗总约束：每一层的输入数据源必须是上一层“用剩”的外账与内账；当前层一旦命中成功，相关外账/内账就从后续层移除，不允许跨层重复命中，确保各层互斥且最终可回溯总量。
[x] 140. `yb_reconcile_demo` 固定“记账对账”高置信层的门诊/住院边界：绿色层与黄色层默认都要严格区分门诊和住院，门诊外账只和门诊内账匹配，住院外账只和住院内账匹配，不允许在高置信与复核层直接做门诊/住院混配。
[x] 141. `yb_reconcile_demo` 固定“记账对账”分层顺序：必须先跑“同日且总账完全对上”，再跑“跨天但总账完全对上”，随后才允许进入“同日差额可解释”“跨天差额可解释”等较弱层级；只要更强层还能找到解，就不能提前落到弱层，避免出现“差 13 可解释但同池仍有 13 未使用”的冲突。
[x] 142. `yb_reconcile_demo` 固定“记账对账”风险降级条件：同名同日仅一笔外账时风险较低；同名同日多笔外账、缺少身份证辅助识别、存在多个等价组合解、或虽能对齐但责任链不唯一时，不应直接进入绿色层，而要降级到黄色/橙色人工复核层。
[x] 143. `yb_reconcile_demo` 固定“记账对账”特殊层与最终层边界：紫色层只承接前面各层剩余数据中的特殊模式（如预约挂号跨多日、特殊小额、可疑重复来源等），可放宽规则但必须保留门诊/住院标签；红色层只承接最终仍未解释成功的剩余数据，并在展示与统计时继续区分门诊/住院。
[x] 144. `yb_reconcile_demo` 固定“记账对账”中间结果落库规模：首版先按 21 张中间结果表 / 21 份参考 CSV 设计，不再额外按门诊/住院物理拆表；门诊/住院差异先通过字段与前端标签区分，避免中间表数量过早膨胀。
[x] 145. `yb_reconcile_demo` 固定“记账对账”中间结果命名规范：为和“自付对账”的 `lyt_*` 表区分，记账漏斗全部改用 `zsh_` 前缀；表名尽量采用短缩写，不直接把“同日差额可解释”等长中文语义整段拼进英文表名。
[x] 146. `yb_reconcile_demo` 规划“记账对账”21 张表命名方案：建议按 `zsh_sd_ok_*`（同日完全对上）、`zsh_xd_ok_*`（跨天完全对上）、`zsh_sd_exp_*`（同日差额可解释）、`zsh_xd_exp_*`（跨天差额可解释）、`zsh_review_*`（橙色复核）、`zsh_special_*`（紫色特殊）、`zsh_final_*`（红色最终）等层命名，并统一使用 `success / fail_his / fail_dsf` 后缀。
[x] 147. `yb_reconcile_demo` 固定“记账对账”21 张表的层间输入输出链：原始内外账先进入 `zsh_sd_ok_*`；其 `fail_his / fail_dsf` 进入 `zsh_xd_ok_*`；再依次进入 `zsh_sd_exp_*`、`zsh_xd_exp_*`、`zsh_review_*`、`zsh_special_*`、`zsh_final_*`。各层统一遵守“下一层只吃上一层用剩的数据”，并同步输出同名 CSV。
[x] 148. `yb_reconcile_demo` 固定“记账对账”21 张表与 CSV 的对应关系：`zsh_sd_ok_success/fail_his/fail_dsf`、`zsh_xd_ok_success/fail_his/fail_dsf`、`zsh_sd_exp_success/fail_his/fail_dsf`、`zsh_xd_exp_success/fail_his/fail_dsf`、`zsh_review_success/fail_his/fail_dsf`、`zsh_special_success_his/success_dsf/fail_his/fail_dsf`、`zsh_final_fail_his/fail_dsf`；参考文件统一使用与表名一致的 `.csv` 文件名。
[x] 149. `yb_reconcile_demo` 固定“记账对账”每层摘要文件命名：除 21 张表对应 CSV 外，每层额外输出 1 份 `summary.txt`（如 `zsh_sd_ok_summary.txt`、`zsh_xd_ok_summary.txt`、`zsh_sd_exp_summary.txt`、`zsh_xd_exp_summary.txt`、`zsh_review_summary.txt`、`zsh_special_summary.txt`、`zsh_final_summary.txt`），用于记录本层输入量、命中量、剩余量与关键口径说明。
[x] 150. `yb_reconcile_demo` 固定“记账漏斗”实施边界：首版先并行建设 `zsh_*` 新链路，不直接替换当前“记账对账”正式接口；待新链路和新页面经过大量实验样本验证稳定后，再决定隐藏或删除旧页面。
[x] 151. `yb_reconcile_demo` 固定“记账漏斗”字段结构策略：21 张中间结果表首版尽量沿用当前记账接口与中间结果表的现有字段结构，不在这一轮先扩大字段范围；“命中明细是否拆子表存储”留到后续单独讨论。
[x] 152. `yb_reconcile_demo` 固定“记账漏斗”验证前提：当前已准备实验样本，后续每层口径、降级规则和页面呈现都要基于这些样本逐层回放验证，不允许只凭抽象规则直接替换现有生产可用页面。
[x] 153. `yb_reconcile_demo` 固定“记账对账”第 5 层（`zsh_review_*`）的降级规则：前 1~4 层只接“高置信唯一解”；只要出现以下任一条件，即使金额能对上，也不得进入绿色/黄色成功层，而要降到复核层：`same_name_same_day_outer_count > 1`、`solution_count > 1`、`has_same_day_and_cross_day_solution = true`、`public_expense_without_id_risk = true`、`inner_conflict_outer_count > 1`、`combo_size >= 4`、`same_name_multi_person_suspected = true`。
[x] 154. `yb_reconcile_demo` 固定“记账对账”成功结果存储模型：采用“主表 + 关系表”两层结构，不在主成功表里硬塞全部命中链。各层 `zsh_*_success` 主表负责存外账视角摘要、层级状态、风险标记、解释码与命中内账数量。
[x] 155. `yb_reconcile_demo` 规划统一关系表 `zsh_match_relation`：用于存储“某一笔成功外账在某一层对应了哪一笔或哪些笔内账”的关系链；字段至少包含 `success_table_name`、`success_id`、`layer_code`、`outer_key`、`inner_key`、`relation_seq`、内外账姓名、时间、就诊类型、总额、记账等关键回溯字段，支持前端展开明细时按成功记录反查命中链。
[x] 156. `yb_reconcile_demo` 固定“记账对账”前端明细读取方式：列表页先查各层 `zsh_*_success` 主表，用户展开某条成功记录时，再按 `success_table_name + success_id` 查询 `zsh_match_relation`，展示“一笔外账对应了一笔或多笔内账”的可追溯关系。
[x] 157. `yb_reconcile_demo` 固定“记账漏斗”关系表主键定位方式：统一使用 `success_table_name + success_id` 联合定位成功记录，不额外引入跨全链路的全局 success_id。
[x] 158. `yb_reconcile_demo` 固定“记账漏斗”可解释差额池首版口径：`13`、`21`、`26`、`35`；这些值可进入 `zsh_sd_exp_*` / `zsh_xd_exp_*` 层，`42` 等其它特殊金额暂不纳入 explain 层，先留给紫色特殊层。
[x] 159. `yb_reconcile_demo` 固定“记账漏斗”跨天窗口：`zsh_xd_ok_*` 与 `zsh_xd_exp_*` 两层首版统一使用 `±1 天` 时间窗口，不先拆成 30 分钟 / 25 小时等更细粒度阶段。
[x] 160. `yb_reconcile_demo` 固定“记账漏斗”新链路入口：首版不复用旧“记账对账”接口，新增独立后端接口 `POST /api/reconcile/book-funnel`，在新页面与实验样本验证稳定前不替换旧页面。
[x] 161. `yb_reconcile_demo` 已在 `yb_reconcile_demo/表结构/zsh_book_funnel_tables.sql` 中补齐“记账漏斗”首版大建表 SQL：包含 21 张 `zsh_*` 中间结果表和 1 张统一关系表 `zsh_match_relation`，默认在 `his_inner` 库执行。
[x] 162. `yb_reconcile_demo` 已新增“记账漏斗”独立后端入口 `POST /api/reconcile/book-funnel`，当前先不替换旧“记账对账”接口，作为实验链路独立返回结果。
[x] 163. `yb_reconcile_demo` 已落第一阶段“记账漏斗”后端骨架：首版先实现 `zsh_sd_ok_*`（同日完全对上）与 `zsh_xd_ok_*`（跨天完全对上）两层，按“上一层用剩数据进入下一层”的规则运行，并将结果清空重写到对应中间表中。
[x] 164. `yb_reconcile_demo` 已接“记账漏斗”统一关系表写入：首版在成功落表时同步将命中的“一笔外账对应一笔或多笔内账”写入 `zsh_match_relation`，前端后续可按 `success_table_name + success_id` 反查命中链。
[x] 165. `yb_reconcile_demo` 已为“记账漏斗”前两层补参考输出：在 `outputs/book_funnel/` 下生成 `zsh_sd_ok_*` 与 `zsh_xd_ok_*` 对应的 CSV/TXT，便于先脱离前端核对实验结果。
[x] 166. `yb_reconcile_demo` 已把“记账漏斗”前五层首版后端骨架接齐到 `/api/reconcile/book-funnel`：当前已串起 `zsh_sd_ok_*`、`zsh_xd_ok_*`、`zsh_sd_exp_*`、`zsh_xd_exp_*`、`zsh_review_*` 五层的顺序执行、剩余数据递传、首版风险降级、清空重写落库、关系表写入与 `outputs/book_funnel/` 参考 CSV/TXT 输出，但 `zsh_special_*` / `zsh_final_*` 仍待后续继续接入。
[x] 167. `yb_reconcile_demo` 已完成“记账漏斗”首轮代码自检：`book_funnel_store.py`、`reconcile_engine.py`、`router.py`、`reconcile.py` 当前均已通过 `py_compile` 语法检查，可进入实验样本验证阶段。
[x] 168. `yb_reconcile_demo` 已新增“记账漏斗”合成样本回归脚本 `yb_reconcile_demo/scripts/book_funnel_sample_check.py`：当前覆盖同日完全对上、跨天完全对上、同日差额可解释、跨天差额可解释、橙色复核层、门诊/住院严格分流与最终未命中等样本；首轮跑数结果与预期一致，可作为后续调规则时的快速回归基线。
[x] 169. `yb_reconcile_demo` 已补强“记账漏斗”复核降级条件并通过样本回归：当前前五层已把“多个等价最优解（`solution_count > 1`）”与“公费同名同日多外账导致的无身份证风险 / 疑似同名不同人”纳入 `zsh_review_*` 的首版降级规则；同时修正成功主表 `risk_flags / explain_code` 不再写死，`book_funnel_sample_check.py` 回归结果仍与预期一致。
[x] 170. `yb_reconcile_demo` 已把“记账漏斗”后两层接齐：当前 `/api/reconcile/book-funnel` 已新增 `zsh_special_*`（紫色特殊层）与 `zsh_final_*`（红色最终层）的首版后端逻辑、清空重写落库与 `outputs/book_funnel/` 参考 CSV/TXT 输出。首版紫层按特殊金额池（`13/21/26/35`）从 `zsh_review_*` 剩余数据中单独沉淀，其余直接进入最终失败层。
[x] 171. `yb_reconcile_demo` 已扩展“记账漏斗”合成样本回归到全链路：`book_funnel_sample_check.py` 当前新增紫层 / 红层样本，已覆盖 `sd_ok / xd_ok / sd_exp / xd_exp / review / special / final` 全链路；最新回归结果与预期一致，说明首版 7 层闭环已跑通。
[x] 175. `yb_reconcile_demo` 已修正“记账漏斗”两处前后端对齐问题：`BookFunnelReconcile.vue` 的进度轮询地址已改为 `/api/reconcile/book-funnel/progress`，后端新增对应进度接口；同时修正 `xiaochu` 层按 `encounter_type` 拆分门诊/住院时误按英文值判断的问题，避免出现“蓝色消除层门诊/住院全为 0、但下一层仍继续消费内账”的统计错位。
[x] 176. `yb_reconcile_demo` 已收紧“记账漏斗”第 6 层门诊自净口径：对进入 `xiaochu` 层的门诊剩余内账，凡“总金额=0 且 记账金额=0”的记录，直接归入 `zsh_xiaochu_success_mz`，不再继续跌落到 `special/final` 剩余池。
[ ] 182. 记录“记账漏斗”当前已知边界：为支持 `xd_ok / xd_exp` 的 `±1 天` 候选窗口，内账在读取时会相对用户选定时间范围向前后各扩一天；若这些扩窗内账最终未被前面层级使用，当前可能继续流入 `special / final` 并在页面上显示为“剩余内账 / 最终剩余”，从而出现“不属于用户原始查询区间的辅助内账也被展示成死账”的现象。当前先记录，不立即处理。
[x] 183. `yb_reconcile_demo` 已将 `zifu_zuhe.py` 的核心思路适配进项目自付主链：新增门诊组合层 `lyt_zuhe_success / lyt_zuhe_fail_his / lyt_zuhe_fail_dsf`，逻辑为“门诊 25h 剩余内账按同一人同一天聚合后，以 `SUM_PAYMENT` 合计按 对账日期 + 金额 精确匹配住院 25h 剩余外账”；该层只作用于门诊，不作用于住院。
[x] 184. `yb_reconcile_demo` 已调整自付主链的后续输入：`内部冲销` 不再直接吃 `mz_25h_inner_unmatched / zy_25h_outer_unmatched`，而改为吃 `lyt_zuhe_fail_his / lyt_zuhe_fail_dsf` 的剩余数据；同步新增 `yb_reconcile_demo/sql/create_self_pay_zuhe_tables.sql` 与 `outputs/self_pay/lyt_zuhe_*.csv/.txt` 参考输出。
[x] 185. `yb_reconcile_demo` 已补齐自付执行页对“门诊组合层”的前端展示：`SelfPayReconcile.vue` 当前已将 `zuhe_result` 插入“住院25时”与“内部冲销”之间，步骤条升级为“自付十一层漏斗”，并同步修正完成态步数与局部分页更新分支。
[ ] 177. `yb_reconcile_demo` 规划 `/book/funnel` 前端独立分页改造：首版不做“全局同页码”假分页，而是按“层 + 分组（success / fail_his / fail_dsf / xiaochu 子分组等）”支持独立翻页；先冻结实施方案，不立即开工。
[ ] 178. `yb_reconcile_demo` 规划 `/book/funnel` 分页后端接口模型：保留当前 `/api/reconcile/book-funnel` 作为摘要与首屏结果入口，后续新增“按 layer + bucket 查询明细分页”的只读接口，避免一次请求同时重切 27 个区块的页码。
[ ] 179. `yb_reconcile_demo` 规划 `/book/funnel` 前端分页状态结构：每个区块单独维护 `page/page_size`，建议使用 `layer.bucket` 作为 key 做 `sessionStorage` 持久化，避免翻某一块时影响其它层。
[ ] 180. `yb_reconcile_demo` 规划 `/book/funnel` 前端逐块接入顺序：先接前 1~5 层的 `success/fail_his/fail_dsf`，再接 `xiaochu` 六块、`special` 四块、`final` 两块；每块统一用 `el-pagination`，并保留当前摘要卡片不变。
[ ] 181. `yb_reconcile_demo` 规划 `/book/funnel` 分页验收方式：用固定实验样本与一轮真实数据回放，确认“任意一个区块翻页时，其他区块摘要与页码不被连带改写”，且接口返回的 `total/page/page_size` 与数据库中间表计数一致。
[x] 172. `yb_reconcile_demo` 记录“自付对账”层级结构已重排，后续开发与讨论统一以新顺序为准：`层1 门诊绿色(60s)` → `层2 住院绿色(120s)` → `层3 宽松匹配（直接吃层2剩余外账 × 层1剩余门诊内账，不遵守映射）` → `层4 门诊30m` → `层5 住院30m` → `层6 门诊25h` → `层7 住院25h` → `层8 内部冲销（zy + mz + dsf 三合一）` → `层9 特殊数字` → `层10 最终死账`。此前关于“黄色层/蓝色层/紫色层”的旧顺序描述仅保留历史背景，不再代表当前主链路。
[x] 173. `yb_reconcile_demo` 自付对账前端顺序已与新 10 层主链路对齐：`SelfPayReconcile.vue` 中将“黄色容错层”提前到两个绿色层之后，后续的 30 分钟 / 25 小时 / 内部冲销 / 特殊数字 / 最终死账按新顺序重排；同步把进度条文案改为“自付十层漏斗”，并将“宽松60s”统一改名为“宽松匹配”。
[x] 174. `yb_reconcile_demo` 已统一“记账漏斗”前端业务文案：`BookFunnelReconcile.vue` 中将“第三方”统一改回“外账”，将“未命中”统一改成“剩余内账 / 剩余外账”，并将“橙色复核 / 紫色特殊 / 红色死账”等颜色名改成业务层名（如“人工复核 / 特殊模式 / 最终剩余”）；页面右上角新增说明入口，明确每一层的业务含义和“下一层只吃上一层剩余数据”的约束。
[x] 177. `yb_reconcile_demo` 已修正“记账漏斗”自净层（`xiaochu`）落库与导出：在 `book_funnel_store.py` 修复了自净层误调用紫层插入函数导致的 DB 列不匹配（`offset_code`与`special_code`冲突）错误，为其专门实现落库逻辑；并同步在 `reconcile_engine.py` 补齐该层的 6 张 CSV CSV 与 `summary.txt` 的本地落盘。
[ ] 186. 记录前端已知交互边界：`/book/funnel` 与 `/self-pay/run` 在“执行对账进行中”时，如果用户切到其它菜单/标签后再返回，当前页面只会恢复日期表单，不会恢复“执行中”的进度面板；原因是两个页面目前仅通过 `sessionStorage` 恢复表单、已完成结果和部分 tab 状态，未持久化 `loading / progressState`，也未在重新挂载时主动接回正在执行的轮询状态。该问题先记录，后续再统一修补。
[ ] 187. `yb_reconcile_demo` 调整“记账统计”数据源口径：`/book/statistics` 后续应读取 `/book/funnel` 生成的 `zsh_*` 漏斗结果与统计数据，不再继续复用旧版 `/book` 接口和旧 1~8 类结果，避免统计页与当前实验链路口径脱节。
[ ] 188. `yb_reconcile_demo` 规划两个统计页的布局与指标：需要分别设计 `/book/statistics` 与 `/self-pay/statistics` 的页面结构、卡片区、分布图区、风险池/剩余池区、关联明细区，并明确每页优先展示哪些层级摘要、来源分布、金额分布、时间分布、门诊/住院分布及阶段漏斗转化数据。
[ ] 189. `yb_reconcile_demo` 规划 `/combined` 实现路径：首版直接复用 `/book/funnel` 与 `/self-pay` 已落库的中间结果和统计数据，不重复跑匹配；但要先设计关联规则，明确如何按人名/日期/就诊类型/层级状态串联“记账结果”和“自付结果”，并识别“同一轮执行”与“不同轮执行”的边界。
[x] 190. `yb_reconcile_demo` 已落地 Text2SQL 智能图表 v1：后端已将 `scripts/text2sql_demo.py` 的第一层 SQL 生成/校验/执行能力抽到正式 service，并新增 `POST /api/text2sql/query` 与 `GET /api/text2sql/catalog` 两个接口；第二层会基于 `question + final_sql + columns + rows` 生成受控的图表结果，当前支持 `bar / line / pie / scatter / table` 五种输出，并在模型返回不稳定时自动降级为表格。前端已新增独立页面 `/text2sql`、表范围选择、SQL/意图回显、ECharts 图表展示与原始表格区；当前查询范围仍严格受 `app/db_schema/*.yaml` 控制，不直接放开全库自由查询。
[ ] 191. `yb_reconcile_demo` 记录新的记账业务现象：同一笔实际内账可能对应多笔外账分摊报销（例如职工三级表 `sbb_zgsj`、职救三级表 `sbb_zjsj` 各承担一部分记账金额，最终共同对应同一笔内账）；后续需要将外账侧的 `jiu_yi_deng_ji_hao` 提升为比 `xing_ming / can_bao_ren_xing_ming` 更高优先级的强匹配键，并评估是否要把“单笔外账进漏斗”改为“同登记号外账先归并后再进漏斗”，以支持“多笔外账 -> 一笔内账”的匹配形态。
[x] 192. `yb_reconcile_demo` 已调整医保 + 公费统一的门诊/住院判定口径：当前优先基于“入院日期 / 出院日期”候选字段判断；当 `出院日期 != 入院日期` 时直接视为住院，当 `出院日期 == 入院日期` 时视为门诊；若表内缺少这两类日期字段，则自动回退到原有 `encounter_rule`（公费用 `zhu_yuan_tian_shu`、医保用文本业务类型）继续分流。
[x] 193. `yb_reconcile_demo` 已落地住院多层报销的首版漏斗方案：`book-funnel` 当前已在 `sd_ok` 与 `xd_ok` 之间新增正式层 `zy_group`，只处理 `sd_ok` 剩余中的住院样本；按 `登记号 + 姓名 + 同账单日` 聚合同一次住院的多笔外账，在“组内总账一致”前提下合并记账金额后继续与住院内账做同日精确匹配。同步已补齐 `zsh_zy_group_*` 结果表、`zsh_outer_group_relation` 回溯关系表、`outputs/book_funnel/zsh_zy_group_*` 导出、`/api/reconcile/book-funnel` 返回结构、`/book/funnel` 九层前端展示，以及 `book_funnel_sample_check.py` 的 `zy_group` 合成样本回归。
[x] 194. `yb_reconcile_demo` 已补充“记账漏斗结果表”全量重建脚本：[sql/rebuild_zsh_book_funnel_tables.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/rebuild_zsh_book_funnel_tables.sql)。当 `zsh_*` 结果表仍是旧版本、与当前 `zy_group` 字段/关系表定义不一致时，可直接在结果库执行该脚本做 `DROP + CREATE` 全量重建；后端报错提示也已同步指向这份脚本。
[x] 195. `yb_reconcile_demo` 已将“第二层聚合”从当前仅处理住院的 `zy_group` 升级为“登记号聚合层”：门诊与住院只要外账稳定存在登记号，都会进入第二层处理“多笔外账 -> 一笔内账”形态；当前口径已按“登记号 + 姓名 + 同账单日 分组，组内总账必须一致，仅合并记账金额，未命中继续跌落 `xd_ok`”落地。当前先保留 `zy_group` 代码名与表名，后续再评估是否统一重命名为更中性的 `reg_group / visit_group`。
[ ] 196. `yb_reconcile_demo` 规划住院记账内账标准化脚本：后续补一张类似 `lyx_mz_zifu_detail` 的住院标准化表，首版来源为 `ip_settlebill_master + ip_inpatient_register`，至少统一出“患者姓名 / 结算时间 / 总金额 / 记账金额 / 自付金额 / FREGISTER_NO / FIP_SERIALNO / 业务主键”等字段；本轮先记录待办，不立即写脚本。
[ ] 197. `yb_reconcile_demo` 规划住院自付内账标准化脚本：后续补 `ip_prepay_detail + ip_inpatient_register` 的住院押金标准化表，供自付对账统一消费；本轮先记录待办，不立即写脚本。
[x] 198. `yb_reconcile_demo` 已调整住院记账内账查询与匹配优先级：`_query_inpatient_inner_records()` 当前已正式带出 `ip_settlebill_master.FREGISTER_NO`，并在“记账-住院”路径里优先使用“外账 `jiu_yi_deng_ji_hao` 系列字段 == 内账 `FREGISTER_NO`”做精确匹配；若未在候选住院内账中找到相等的 `FREGISTER_NO`，则降级回“姓名 + 时间 + 金额”的现有住院匹配方式。同步已补合成样本，覆盖“登记号精确命中 / 内账登记号为空可降级 / 内账登记号非空但未命中时仍可降级”三类边界。
[x] 199. `yb_reconcile_demo` 已调整门诊/住院分流的结束日期候选字段优先级：`chu_yuan_ri_qi` 当前已高于 `jie_suan_ri_qi`；只有当前表结构里不存在 `chu_yuan_ri_qi` 时，才会继续尝试 `jie_suan_ri_qi`，避免把结算日期过早当成住院结束日期。
[x] 200. `yb_reconcile_demo` 已落地“记账漏斗”中的“门诊外账 -> 住院内账”登记号交叉映射层：当前收敛为单向纠偏，不做双向“交叉对账”。层位置已放在 `1. 同日完全对上(sd_ok)` 之后、`2. 登记号聚合(zy_group)` 之前；输入只消费 `sd_ok` 剩余中的“外账判定为门诊、且 `group_key_value` 非空”的外账，候选内账只看“未匹配的住院内账、且 `FREGISTER_NO` 非空”的子集，并按“外账 `jiu_yi_deng_ji_hao` 系列字段 == 内账 `FREGISTER_NO`”做同日 exact 匹配。命中的外账/内账会在本层立即消费，不再进入后续层；未命中的继续跌落到 `zy_group`，层与层之间保持无重叠。当前已同步接入 `zsh_cross_register_success / zsh_cross_register_fail_his / zsh_cross_register_fail_dsf` 结果表、对应 CSV / summary TXT、`/api/reconcile/book-funnel` 返回结构、`/book/funnel` 步骤条与独立 tab，并补了合成样本 `O17 -> cross_register` 回归验证。
[ ] 201. `yb_reconcile_demo` 评估外账统一标准化落表的收益与边界：当前医保 / 公费外账在 `reconcile_engine._query_outer_records()` 中运行时并发扫表、逐表取数并在内存整合，第三方支付在 `third_party_normalizer.load_normalized_third_party_rows()` 中逐表标准化后再在内存合并；后续需要评估是否提前写脚本把医保 / 公费 / 第三方支付统一落成标准化表，以换取运行耗时、内存占用、可追溯性和统计可复用性的提升。
[x] 202. `yb_reconcile_demo` 已将“住院记账内账”查询切换到标准化表 `lyx_zy_jizhang_detail`：当前 `reconcile_engine._query_inpatient_inner_records()` 不再实时读取 `IP_SETTLEBILL_MASTER + IP_INPATIENT_REGISTER`，而是直接查询 `lyx_zy_jizhang_detail`，按 `fpat_name + fsettle_date` 过滤，并映射 `fsum_total / fsum_receivable / fregister_no / fip_serialno` 到现有 `InnerRecord`。这样已把“姓名 + 时间 + 金额 + 登记号”固定在单表中，减少联表和字段缺失不一致带来的风险，同时保持当前“先 `FREGISTER_NO` 精确匹配，未命中再降级为姓名+时间+金额”的住院记账逻辑不变。
[x] 203. `yb_reconcile_demo` 已将“住院自付内账”加载切换到标准化表 `lyx_zy_zifu_detail`：当前 `self_pay_matcher._load_inpatient_prepay_details()` 不再读取 `ip_prepay_detail`，而是直接查询 `lyx_zy_zifu_detail`，用 `id` 作为明细主键、`fip_serialno` 作为住院流水归并键、`ffinancial_date` 作为交易时间、`deposit_amt` 作为匹配金额，并保留 `fpat_name / fsettle_date / fsum_total / fsum_receivable / fsum_payment / deposit_type` 供导出与后续规则使用。当前已固化金额符号规则：`deposit_type='退回'` 在代码里一律按负数参与计算；其它类型保持表内现有符号口径不变（如 `充值` 视为正数，`取款` 直接沿用表中的负数）。
[x] 204. `yb_reconcile_demo` 已完成“住院记账标准化表接入”的实施清单：已在 `reference_defaults.py` 中新增 `lyx_zy_jizhang_detail` 表常量，并将 `ReconcileEngine._query_inpatient_inner_records()` 的 SQL 来源切换到标准化表，读取 `fip_serialno / fregister_no / fpat_name / fsettle_date / fsum_total / fsum_receivable` 映射回现有 `InnerRecord`，保持 `encounter_type='inpatient'` 与现有匹配逻辑不变；当前 `record_id` 已采用 `fip_serialno + id` 组合防重；样本回归已确认“登记号精确命中 / 未命中登记号后降级 / cross_register / zy_group”在切换数据源后未回归，CSV / 落库 / 页面结果本轮无需额外改表结构。
[x] 205. `yb_reconcile_demo` 已完成“住院自付标准化表接入”的实施清单：已在 `self_pay_matcher.py` 中将住院自付数据源切到 `lyx_zy_zifu_detail`，并扩展 `InpatientPrepayDetail` 结构，补充 `person_name / settle_time / deposit_type / settle_total_amount / settle_book_amount / settle_payment_amount`；当前 `_load_inpatient_prepay_details()` 已按 `id -> fprepaydetail_id`、`fip_serialno -> finvoice_id(继续承载住院流水键)`、`ffinancial_date -> trade_time`、`deposit_amt -> amount` 映射标准化表；金额符号规则已固化为“`deposit_type='退回'` 强制按负数参与计算，其它类型保持表内现有符号不变”。本轮已同步补进住院自付匹配结果和未命中内账 CSV 的导出元信息，且未破坏后续负数对消逻辑。
[x] 206. `yb_reconcile_demo` 已补“医保 / 公费外账标准化落表”首版方案：当前已新增 [sql/create_lyx_waizhang_detail.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/create_lyx_waizhang_detail.sql) 与 [scripts/waizhang_import.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/scripts/waizhang_import.py)，按现有 `reconcile_engine` 的候选字段识别、表内排除规则、门诊/住院判定与登记号提取口径，将 `lyt_26_2_gf` / `lyt_26_2_yb_2` 标准化写入 `his_inner.lyx_waizhang_detail`；当前先补落表脚本与表结构，尚未切换正式链路读取该表。
[x] 208. `yb_reconcile_demo` 当前已完成 `his_inner.lyx_waizhang_detail` 标准化外账表的实际建表
[ ] 209. `yb_reconcile_demo` lyx_waizhang_detail已经建好，但主程序本轮仍保持原策略，`reconcile_engine._query_outer_records()` 继续直接从 `lyt_26_2_gf` / `lyt_26_2_yb_2` 两个库动态识别并读取所有满足候选字段规则的外账表，尚未切换为优先读取 `lyx_waizhang_detail`。
[x] 207. `yb_reconcile_demo` 已落地“记账漏斗”第 1 层 `sd_ok` 的住院宽松版改造：未新增独立层，仍保留在第 1 层内部完成。当前口径为：对住院外账，先取“同名 + 同日 + 住院”候选池，再优先抽取其中 `FREGISTER_NO == 外账 group_key_value` 的子池尝试 exact 匹配；若该“登记号优先子池”未形成“记账和总账都完全对上且无风险”的成功结果，则同一笔外账继续在第 1 层内回退到原始“同名 + 同日 + 住院候选池”按当前组合搜索策略正常跑。门诊路径保持不变，后续 `cross_register / zy_group / xd_ok ...` 层顺序与输入输出边界本轮未改；已补合成样本 `O18`，验证“登记号子池失败后，同层宽松回退仍可进入 `sd_ok`”。
[x] 210. `yb_reconcile_demo` 已取消门诊内账 `SUM_TOTAL = 0.01` 的硬排除：当前 `_query_outpatient_inner_records()` 不再在 SQL 层过滤 `SUM_TOTAL <> 0.01`，原 `INNER_EXCLUDED_SUBINVOICE_SUM_TOTAL` 常量已移除；后续这类 `0.01 / 0.01` 门诊内账会正常进入记账对账候选池，不再因取数入口被提前丢弃。
[x] 211. `yb_reconcile_demo` 已确认“记账漏斗”登记号强匹配总原则：当外账 `group_key_value`（`jiu_yi_deng_ji_hao` 系列字段）非空时，登记号路径应提升为比“姓名 + 时间”更强的前置主线；在这条路径里，姓名与时间不再作为候选前提，只保留作展示字段与异常提示，自动成功与否仅由“登记号是否对上 + 记账金额/总金额是否对上 + 是否存在风险”决定。后续默认约束同步冻结为：1）登记号路径优先于现有 `sd_ok / xd_ok / sd_exp / xd_exp` 等姓名时间链；2）登记号路径失败后，是否允许跌回姓名时间链由后续分层单独显式定义，不再隐式混用；3）凡 `group_key_value` 非空且能稳定锁定到住院内账的样本，不再以“同名 / 同日 / 前后 1 天”作为自动成功的必要条件；4）若住院外账与住院内账的登记号已经相同，则默认认为已锁定到同一笔内外账，此时若记账金额或总金额不相等，不再继续回退到姓名/时间链路，而是直接落入 `zsh_final_fail_*`，并补明确标记（如 `register_exact_amount_conflict`）供人工排查。
[ ] 212. `yb_reconcile_demo` 规划“登记号单笔 exact 层” `reg_exact_single`：输入只吃 `group_key_value` 非空的外账，候选内账直接取“未匹配住院内账中 `register_no == group_key_value`”的集合，不再加姓名/时间限制；首版按“登记号 + 记账金额 + 总金额”做 exact 匹配，命中且无风险则直接成功，未命中的外账继续跌落到下一条登记号路径。
[ ] 213. `yb_reconcile_demo` 规划“登记号聚合 exact 层” `reg_exact_group`：承接 `reg_exact_single` 剩余中的同登记号多笔外账，先按 `group_key_value` 归并，再用聚合后的记账金额 / 总金额去匹配 `register_no == group_key_value` 的住院内账；本层不再追加“姓名 + 同账单日”约束，以适配“多笔外账分摊报销 -> 一笔住院内账”的稳定登记号形态。当前 `zy_group` 后续拟被这层吸收或替换，避免职责重叠。
[ ] 214. `yb_reconcile_demo` 规划“登记号复核层” `reg_review` 及其与旧链路的边界：当外账 `group_key_value` 非空且登记号已锁定到住院内账，但出现“多个等价金额解 / 同登记号多笔内账责任链不唯一 / 登记号对上但总账差异无法自动认定”等情况时，优先沉淀到专属复核层，而不是立即回退到“姓名 + 时间”链路。后续常规 `sd_ok / cross_register / xd_ok / sd_exp / xd_exp / review ...` 只建议处理“登记号为空”或“业务明确允许降级”的外账，层间继续保持互斥消费。
[ ] 215. `yb_reconcile_demo` 规划在今日稍后重构“记账对账”的整体漏斗形态：现状是一条门诊/住院混合漏斗，但两类业务的匹配规则并不完全一致；后续拟拆成“门诊漏斗”和“住院漏斗”两条独立主链，各自完成本类别对账、分层与剩余沉淀。只有在涉及“外账门诊 -> 内账住院”的特殊纠偏场景时，才允许出现跨漏斗读取住院内账的动作；除此之外，门诊与住院应各走各自漏斗，避免高置信层混流。
[x] 216. `yb_reconcile_demo` 已处理住院内账“同一住院登记号下多条记录并存”的有效性判定问题：当前已确认同一 `FREGISTER_NO` 下可能同时存在多条内账，但其中并非全部有效；无效/残缺记录的典型特征是虽然保留了住院登记号，但“结算流水号”为空，并未真正形成有效结算。当前已通过“结算流水号”等辅助字段将这类残缺记录识别并排除，避免其继续干扰住院登记号匹配与多解判断。
[x] 217. `yb_reconcile_demo` 已收紧医保外账日期字段候选口径：当前 `OUTER_SOURCE_RULE_DEFAULTS['yb']` 中，结算日期候选 `date_field_candidates` 已去掉 `chu_yuan_ri_qi`，出院日期候选 `discharge_date_field_candidates` 已去掉 `jie_suan_ri_qi`，避免把“出院日期”和“结算日期”在同一优先链里混用。
[x] 218. `yb_reconcile_demo` 已调整外账标准化与住院时间对齐规则：`waizhang_import.py` / `reconcile_engine.py` 当前已允许外账 `bill_date` 为空；当外账表缺少结算日期候选字段时，导入不会报错，`bill_date` 留空而保留 `discharge_date`。住院链时间比对现已固定为：外账 `bill_date` 有值时，用它对内账 `fsettle_date`；仅当外账 `bill_date` 为空时，才改用外账 `discharge_date` 对内账 `fout_date`。
[x] 219. `yb_reconcile_demo` 已修复“记账漏斗”结果落库的空日期问题：当前 `reconcile_engine.py` 与 `book_funnel_store.py` 已统一把 success / fail_his / fail_dsf / special / xiaochu / relation 表中的空日期字符串归一化为 `None`，避免再次出现 `Incorrect date value: '' for DATE 列` 的落库错误。
[x] 220. `yb_reconcile_demo` 已扩展“记账漏斗”可解释差额池与特殊金额池：当前除 `13 / 21 / 26 / 35` 外，`12`（代码层表现为 `1200`，负值通过绝对值自动覆盖）也已纳入 `BOOK_FUNNEL_EXPLAIN_DIFF_CENTS` 与 `BOOK_FUNNEL_SPECIAL_CENTS`，因此 `sd_exp / xd_exp / special` 三层都会识别 `12.00 / -12.00`。
[x] 221. `yb_reconcile_demo` 已将 `lyt_2_yue` 单独标准化落表到 `his_inner.lyx_dsf_detail`：当前已补 [sql/create_lyx_dsf_detail.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/create_lyx_dsf_detail.sql) 与 [scripts/dsf_detail_import.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/scripts/dsf_detail_import.py)，只读取第三方库 `lyt_2_yue`，并按程序运行时的内存标准化结构 `NormalizedThirdPartyRow` 一对一落表。当前 `lyx_dsf_detail` 字段已与 `record_id / source_code / source_name / source_name_display / table_name / trade_time / amount / direction / raw_amount_field / raw_amount_value / payment_method / transaction_type / trade_status / card_no / card_type_name / issuer_name / trade_no / merchant_order_no / bank_order_no / counterparty_account / counterparty_name / product_name / remark / raw_time` 对齐，不再混入医保/公费外账超集字段。
[x] 222. `yb_reconcile_demo` 已将“215 双漏斗改造”方案整理进 [plan.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/plan.md)：当前文档已固定为 `1. 接口演进策略 / 2. 双漏斗业务设计 / 3. 结果落地与命名规范 / 4. 分阶段实施与验收` 四段正式结构，不再保留聊天记录式拼接内容。
[x] 223. `yb_reconcile_demo` 已在 [plan.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/plan.md) 补齐“逐层 SQL/DDL 规划”：当前已明确 `zsh_op_* / zsh_zy_* / zsh_cross_*` 的逐层职责、输入输出链、命名规范，以及每层的 `3 个建表 SQL（success_ddl / fail_his_ddl / fail_dsf_ddl） + 4 个执行 SQL（input / success / fail_his / fail_dsf）` 设计口径。
[x] 224. `yb_reconcile_demo` 已冻结“215 双漏斗”的基础数据源与字段约定：记账外账统一使用 `his_inner.lyx_waizhang_detail`，门诊内账使用 `his_inner.op_subinvoice_2026`，住院内账使用 `his_inner.lyx_zy_jizhang_detail`；外账以标准化后的 `encounter_type` 作为门诊/住院入口分流依据。当前已在方案中固定门诊字段映射 `PRINTINV_PAT_NAME / DATE(LOGLADATE) / SUM_TOTAL / SUM_RECEIVABLE` 对应外账 `person_name / bill_date / total_amount / book_amount`，住院字段映射 `fregister_no / fpat_name / fsettle_date / fsum_total / fsum_receivable` 对应外账 `group_key_value / person_name / bill_date / total_amount / book_amount`；并同步固定住院日期规则：外账 `bill_date` 为空时，才用外账 `discharge_date` 去对内账 `fout_date`，否则仍以 `fsettle_date` 为主。
[x] 225. `yb_reconcile_demo` 已将第三方标准化读取优先切换到 `his_inner.lyx_dsf_detail`：当前 `third_party_normalizer.load_normalized_third_party_rows()` 已改成先检测 `lyx_dsf_detail` 是否存在；若存在，则直接从该表读取并还原为程序运行时使用的 `NormalizedThirdPartyRow` 结构，供 `self_pay_matcher` 等现有链路复用；若该表不存在，则自动降级回原先直连 `lyt_2_yue`、按 `THIRD_PARTY_SOURCE_CONFIGS` 逐表标准化的旧方案，保证老链路不被硬切断。
[x] 226. `yb_reconcile_demo` 已在当前 `book-funnel` hub 完成首轮“挂号型特殊金额跨日借用”修复：本轮未改双漏斗、接口和表结构，只调整 `reconcile_engine.py` 的现有匹配逻辑。当前已新增“挂号型特殊金额”识别与单向时间约束：对 `12 / 13 / 26 / 35` 中总账命中且记账金额为 `0` 的内账，禁止被更早日期的外账向后借用；同时已将 `review` 层的候选优先级改成 `同日 exact` > `同日 explain` > `跨天 exact` > `跨天 explain`，并在进入 `review` 前按该优先级排序处理剩余外账，减少“先遍历先抢占”造成的资源污染。同步已补 `book_funnel_sample_check.py` 的专项回归，确认原有合成样本未回归，且“未来挂号不能借给过去 / 同日 exact 优先于同日 explain”两条新规则均已生效。
[x] 227. `yb_reconcile_demo` 已补 README 附录，保留“聚合脚本来源口径”的大段原始字段说明：当前 [README.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/README.md) 已新增“附录 A：聚合脚本来源口径”，专门说明 `lyx_waizhang_detail` 如何由 `OUTER_SOURCE_RULE_DEFAULTS` 的医保 / 公费候选字段、就诊分流规则、日期判定规则生成，以及 `lyx_dsf_detail` 如何由 `THIRD_PARTY_SOURCE_CONFIGS` 和时间/金额格式规范生成。主文继续说明“聚合表如何被单漏斗消费”，附录则保留“这些聚合表是怎么从原始表长出来的”，供后续维护 `waizhang_import.py / dsf_detail_import.py` 时参考。
[x] 228. `yb_reconcile_demo` 已完成当前单漏斗的两项快修：1）`/book/funnel` 前端执行中状态现在会持久化“运行中”标记和最近一次进度，页面切走再回来时会重新接回 `/api/reconcile/book-funnel/progress` 轮询，并在后端显示完成后自动补拉最新结果，不再只有执行结束后才能恢复界面；2）第 9 层 `special` 已改成“只隔离特殊内账”，外账即使命中特殊金额也不再进入 `special_success_dsf`，而是继续保留到后续死账链路。当前已完成 `reconcile_engine.py` 语法自检；前端构建在本机环境下因 PowerShell `ExecutionPolicy` / Vite `spawn EPERM` 未能完成，不作为本轮代码逻辑失败。 
[x] 229. `yb_reconcile_demo` 已在当前单漏斗中新增第 10 层 `bookonly` 记账保底层，并把主链调整为 `special -> bookonly -> final`：当前 `bookonly` 只消费 `special` 剩余数据，按现有候选池与组合搜索规则寻找“记账金额精确对上、但总账差额仍不可解释”的无风险样本；同日优先于跨天，且继续遵守住院登记号优先过滤、挂号型特殊金额单向时间约束与多解风险拦截。同步已补 `zsh_bookonly_success / zsh_bookonly_fail_his / zsh_bookonly_fail_dsf` 三张结果表的 DDL、`outputs/book_funnel/zsh_bookonly_*.csv/.txt` 导出、`/api/reconcile/book-funnel` 返回结构、`/book/funnel` 前端“10. 记账保底”展示，以及对应的进度步骤映射；本轮已通过 `reconcile_engine.py / book_funnel_store.py` 的 `py_compile` 语法自检。
[x] 230. `yb_reconcile_demo` 已补齐 [frontend/src/views/BookFunnelReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/BookFunnelReconcile.vue) 中遗漏的 `tabs` 配置：此前该文件处于“半更新”状态，帮助文案、加载步骤条和进度映射已包含 `bookonly`，但实际渲染主界面的 `tabs` 数组仍只有 10 层，导致“10. 记账保底”不会出现在页面标签栏。当前已将 `bookonly` 正式插入 `special` 与 `final` 之间，并把 `final` 标签顺延为“11. 最终死账”，使前端主界面与后端返回结构重新对齐。
[x] 231. `yb_reconcile_demo` 已确认“自付对账”当前数据源口径已切到标准化表：第三方外账优先读取 `his_inner.lyx_dsf_detail`，门诊内账读取 `his_inner.lyx_mz_zifu_detail`，住院内账读取 `his_inner.lyx_zy_zifu_detail`；若 `lyx_dsf_detail` 不存在，程序才降级回 `lyt_2_yue` 原始第三方表逐表标准化。当前已复核 `self_pay_matcher.py / third_party_normalizer.py / README.md`，后续本轮所有“组合 / 组合2”改造都应以这三张标准化表为主口径，不再按旧 `ip_prepay_detail` 或 Excel 临时文件设计。
[x] 232. `yb_reconcile_demo` 已补齐并冻结“自付门诊内账”DDL 依据：当前 [sql/his_zifu_detail.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/his_zifu_detail.sql) 已明确 `lyx_mz_zifu_detail` 的表结构，后续自付门诊组合相关逻辑统一使用 `id / SUBINVOICE_ID / PRINTINV_PAT_NAME / SUM_TOTAL / SUM_PAYMENT / PAYMETH_DESC / DESC_TYPE / DESC_MONEY / LOGLADATE` 作为标准字段，不再依赖口头约定；本轮已同步核对运行时代码与 README 自付章节，避免继续出现旧表名残留。
[x] 233. `yb_reconcile_demo` 已将当前自付第 8 层 `zuhe` 扩成“同层先门诊、后住院”的双段执行版：当前仍然只保留一个 `zuhe_result`，但后端执行顺序已改为“先跑现有门诊组合，再拿门诊段剩余外账继续跑住院组合”；两段都在同一层内完成，未额外新增“住院组合1”独立层，因此整体漏斗结构和接口入口未被打散。
[x] 234. `yb_reconcile_demo` 已落地自付 `zuhe` 层住院复刻规则：门诊段继续保持“同一人 + 同一天聚合门诊剩余内账，以 `SUM_PAYMENT` 合计按 日期 + 金额 精确匹配外账”；住院段已采用同样的执行方式，但字段切换为“同一人 + 同一天聚合住院剩余内账，以 `deposit_amt` 合计按 日期 + 金额 精确匹配外账”，时间字段取 `ffinancial_date`，姓名字段取 `fpat_name`，并且明确只消费门诊段剩余外账，避免门诊/住院重复抢同一笔外账。同步已将 `xiaochu / final` 的住院输入改成吃 `zuhe` 后的住院剩余，而不再继续误吃 `zy_25h` 的旧剩余。
[x] 235. `yb_reconcile_demo` 已完成自付 `zuhe` 层结果结构的首轮兼容改造：当前 `zuhe_result` 已拆出“组合命中 / 门诊内账剩余 / 住院内账剩余 / 外账剩余”四类结果，命中结果已补 `combo_scope / inner_type` 等区分信息，前端 [SelfPayReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/SelfPayReconcile.vue) 也已同步把第 8 层文案从“门诊拼单合集”改为“组合层”，避免继续误导为只处理门诊。当前 `lyt_zuhe_success / lyt_zuhe_fail_his / lyt_zuhe_fail_dsf` 仍沿用既有表结构落库，但已通过 `inner_payment_method / DESC_TYPE / PAYMETH_DESC` 等字段保留门诊/住院来源痕迹。
[x] 236. `yb_reconcile_demo` 已在自付后端主链中新增第 9 层 `zuhe2`：位置已放在现有 `zuhe` 后、`blue` 前；当前层目标是承接 `zuhe` 剩余数据，做“时间窗内多笔凑单”的第二轮组合匹配。首版已补齐 `lyt_zuhe2_success / lyt_zuhe2_fail_his / lyt_zuhe2_fail_dsf` 三张结果表对应的落库入口与 [sql/create_self_pay_zuhe2_tables.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/create_self_pay_zuhe2_tables.sql)，并新增 `outputs/self_pay/lyt_zuhe2_summary.txt` 摘要输出。当前前端第 12 层展示仍待后续 `239` 接入。
[x] 237. `yb_reconcile_demo` 已将 [scripts/zuhe2.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/scripts/zuhe2.py) 的核心算法迁进项目运行时，而不是直接调用离线脚本：当前已把“按外账逐笔遍历、限定前后固定时间窗口、同方向金额过滤、按患者分组搜索最多 N 笔组合、以组合笔数/最大时间差/内部时间跨度做评分”的逻辑抽成 `self_pay_matcher.py` 内的运行时函数，并接入当前 API 返回、CSV 导出与结果落库体系。首版 `zuhe2` 先只处理门诊剩余内账与外账，住院版 `zuhe2` 仍保留在 `238` 待办中，不在本轮提前展开。
[x] 238. `yb_reconcile_demo` 已完成自付 `zuhe2` 的双段执行策略：当前 `self_pay_matcher.py` 中第 9 层 `zuhe2` 已改成与 `zuhe` 相同的“同层先门诊、后住院”顺序执行。门诊段继续使用 `PRINTINV_PAT_NAME / LOGLADATE / DESC_MONEY` 在外账前后 `2 小时` 窗口内做最多 `4` 笔组合搜索；住院段承接门诊段剩余外账，改用 `fpat_name / ffinancial_date / deposit_amt` 做同样的时间窗组合搜索。两段共用 `lyt_zuhe2_success / lyt_zuhe2_fail_his / lyt_zuhe2_fail_dsf` 一组结果表与 `zuhe2_result` 返回结构，`xiaochu / special / final` 的输入也已改为承接 `zuhe2` 剩余；本轮已通过 `py_compile` 语法自检，前端第 9 层标签展示仍留待 `239` 处理。
[x] 239. `yb_reconcile_demo` 已完成自付前端从“十一层漏斗”到“十二层漏斗”的接入：当前 [frontend/src/views/SelfPayReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/SelfPayReconcile.vue) 已在 `zuhe` 与 `blue` 之间插入 `zuhe2` 标签，并同步补齐加载步骤条、`tabs`、`tabKeyMap`、层标题编号和详情弹窗的 `layerType` 兼容逻辑；页面顺序现已更新为“绿色/容错/30m/25h/组合/组合2/内部冲销/特殊/最终”。本轮已人工核对前端与后端 `zuhe2_result` 键名一致；前端构建在本机环境下仍受 PowerShell `ExecutionPolicy` / Vite `spawn EPERM` 影响未能完成，不作为本轮代码接入失败。
[ ] 240. `yb_reconcile_demo` 规划自付 `zuhe / zuhe2` 两层的验收样本与回归重点：至少需要验证“门诊组合仍按旧逻辑命中、不被住院段回归影响”“住院组合可在同层第二段正确消费剩余外账”“`zuhe2` 能承接 `zuhe` 剩余继续命中”“同一笔外账不会同时被门诊段和住院段重复消费”“蓝层 / 特殊层 / 最终层的输入剩余量与新层落库数量一致”。
[x] 241. `yb_reconcile_demo` 已补“统计页 / 组合对账”所需的运行批次基础能力：当前已新增 [run_registry_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/run_registry_service.py)，为 `book_funnel` 和 `self_pay` 两条活跃链路统一生成 `run_id`，并在每次执行完成后同时维护 `latest_run.json`、`runs/{run_id}/result.json` 与 `run_registry.json`。当前 `book_funnel` 与 `self_pay` 的返回结果 `meta` 中也已补入 `run_id / finished_at`，便于后续统计页和 `/combined` 明确读取某一轮快照，而不再只能读“最近一次”。
[x] 242. `yb_reconcile_demo` 已完成 latest 结果读取能力的 `run_id` 化改造：当前 [book_funnel_latest_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/book_funnel_latest_service.py) 与 [self_pay_latest_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_latest_service.py) 均已支持“传 `run_id` 读取某次运行快照，不传则继续回退 latest”的兼容模式；后续分页、统计和联合视图都可以在不改动旧调用入口的前提下，开始按批次查看历史结果。
[x] 243. `yb_reconcile_demo` 已新增“统计页 / 联合看板”后端接口与服务骨架：当前 [router.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/api/router.py) 已接出 `GET /api/reconcile/book-funnel/runs`、`GET /api/reconcile/self-pay/runs`、`GET /api/reconcile/book-funnel/statistics`、`GET /api/reconcile/self-pay/statistics`、`GET /api/reconcile/combined` 五个新接口；同时已新增 [book_funnel_statistics_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/book_funnel_statistics_service.py)、[self_pay_statistics_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_statistics_service.py)、[combined_statistics_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_statistics_service.py) 三个专用服务，开始基于运行快照输出统计数据和联合 case 视图。
[x] 244. `yb_reconcile_demo` 已将 `/book/statistics` 从旧 `/book` 口径切到当前 `book-funnel` 活跃链路：当前 [frontend/src/views/ResultStatistics.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/ResultStatistics.vue) 已不再依赖旧 `sessionStorage` 中的 `/book` 结果，而是改为通过新接口读取记账漏斗运行批次列表与统计结果；页面现已具备“选择 `book_funnel` 运行批次 + 汇总卡片 + 漏斗转化 + 风险分布 + 来源/外账表/死账金额分桶”的首版统计展示能力。
[x] 245. `yb_reconcile_demo` 已为 `/self-pay/statistics` 与 `/combined` 落下首版前端可用骨架：当前 [frontend/src/views/SelfPayStatistics.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/SelfPayStatistics.vue) 已接入自付运行批次选择、层级贡献、来源分布、支付方式分布、时间差分布与最终金额分桶；[frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 已改为基于“记账批次 + 自付批次”读取联合 case 看板，按 `person_name + biz_date + encounter_type` 展示 `book_status / self_pay_status / combined_status`。当前 `/combined` 首版已明确保留一个已知边界：由于第三方标准化结构本身没有 `person_name`，自付侧“纯第三方剩余”暂不进入联合 case 列表，而继续在自付统计页单独查看。
[x] 246. `yb_reconcile_demo` 已记录本轮“统计页 / 联合看板”实施过程中遇到的第一个结构性问题：当前结果库里的 `zsh_* / lyt_*` 运行时结果表依旧是“每次执行覆盖”，并不是按 `run_id` 累积留存的历史表，因此 `run_id` 首版实际只能稳定回放“运行快照 JSON”，不能指望从数据库结果表直接恢复任意历史批次的完整分页明细。当前已据此把批次回放主口径落到 `runs/{run_id}/result.json`，后续若要做“历史明细分页仍走数据库表”，需要单独设计带 `run_id` 的结果表或历史归档表。
[x] 247. `yb_reconcile_demo` 已记录本轮“统计页 / 联合看板”实施过程中遇到的第二个结构性问题：第三方标准化结构 `NormalizedThirdPartyRow` 当前并不包含 `person_name`，因此自付侧“纯第三方未命中外账”无法稳定按 `person_name + biz_date + encounter_type` 并入 `/combined` 的联合 case；当前首版实现已显式选择“这部分继续留在 `/self-pay/statistics` 查看，不强行并到 `/combined`”，避免制造伪关联。后续若业务坚持要把这部分纳入联合看板，需要先补“第三方外账如何得到可靠姓名”的规则或辅助映射表。
[x] 248. `yb_reconcile_demo` 已补齐 `self_pay_latest_service.py` 中 `zuhe2_result` 的分页表映射：当前 latest 分页服务已显式接入 `lyt_zuhe2_success / lyt_zuhe2_fail_his / lyt_zuhe2_fail_dsf`，因此自付执行页切到 `zuhe2` 分类后，翻页与筛选请求不再因层级表名缺失而落不到正确结果表。本轮仅收这一处兼容缺口，并已通过 `self_pay_latest_service.py` 的 `py_compile` 语法自检，未继续扩散到统计页或 combined 的其它收口任务。
[x] 249. `yb_reconcile_demo` 已完成“统计页 / 联合看板”最小联调验收的首轮后端验证：本轮已实际检查 `outputs/book_funnel/latest_run.json`、`outputs/self_pay/latest_run.json` 是否存在，并直接调用 `list_runs()`、`load_book_funnel_statistics()`、`load_self_pay_statistics()`、`load_combined_statistics()` 做最小联调。当前验证结论是：1）三个新接口的代码路径可执行，没有 Python 语法级报错；2）`/combined` 能返回完整的 meta / summary_cards / pagination 结构；3）当前最大的阻塞并不是接口炸掉，而是现有 latest 快照仍是“旧结构”，尚未包含 `run_id / statistics_view / combined_side_cases`，因此本地首轮验证中 `book/self-pay` 统计卡片为空、runs 列表为空、`combined` case 数为 0。也就是说，新页面骨架已通，但要看到有效统计数据，必须先生成一轮“新结构快照”或补兼容回填逻辑。
[ ] 250. `yb_reconcile_demo` 待解决：明确这三个模块接下来如何拆步收口。当前改动已同时涉及运行批次、latest 读取、统计接口、联合接口和三个前端页面，单次跨度偏大；下一轮建议优先级明确为“先收后端批次快照与 latest 兼容 -> 再收 book statistics -> 再收 self-pay statistics -> 最后收 combined”，避免继续多线并发导致排查范围过宽。
[x] 251. `yb_reconcile_demo` 已确认当前运行产物已升级到“新结构快照”：现有 `outputs/book_funnel/latest_run.json` 与 `outputs/self_pay/latest_run.json` 已都包含 `run_id / finished_at / statistics_view / combined_side_cases`，对应的 `run_registry.json` 也已有可读批次记录，因此三张新页面不再只能依赖“空 runs 列表 + 空统计结果”的旧状态。后续这一步更多是你在 `desheng` 环境里继续手动重跑和验数，而不是代码层还缺生成逻辑。
[x] 252. `yb_reconcile_demo` 已完成旧版 latest 快照的首轮兼容回填：当前已新增 [statistics_compat_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/statistics_compat_service.py)，当 `statistics_view` 缺失时，`book_funnel/statistics` 与 `self_pay/statistics` 会从现有 `results.*.summary` 运行时补算 `summary_cards / funnel_distribution / layer_summary`；当 `combined_side_cases` 缺失时，`/combined` 也会基于缓存页内已有明细尽量回退生成联合 case。与此同时，[run_registry_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/run_registry_service.py) 现已支持“registry 缺失时用 latest 自动补一条 run 记录”。当前这层兼容的边界也已明确：如果旧快照本身只缓存了分页首屏明细，那么 `/combined` 的回退 case 只能做到“尽量不空白”，精度仍不等同于重跑得到的完整 `combined_side_cases`。
[x] 253. `yb_reconcile_demo` 已为 [frontend/src/views/SelfPayReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/SelfPayReconcile.vue) 补齐“自付漏斗层级逻辑说明”帮助面板：当前 `/self-pay/run` 执行区已新增与 `/book/funnel` 同风格的问号帮助按钮，弹层内按 12 个层级逐条解释“绿色 / 容错 / 30 分钟 / 25 小时 / 组合 / 组合2 / 蓝层 / 紫层 / 红层”的职责，并明确写出 `zuhe / zuhe2` 是“同层先门诊、后住院”，以及蓝层并非常规成功命中而是内部冲销识别，便于项目交接和现场排查时快速理解每层语义。
[x] 254. `yb_reconcile_demo` 已完成 `/combined/case-detail` 独立明细接口的接口口径设计并落地为实际后端返回结构：当前 `/combined` 列表继续只返回病例级摘要，展开明细不再依赖列表接口硬塞 `book_items / self_pay_items`，而是改为预留单独按 case 查询 detail 的模式。新接口已明确区分两类数据：1）已成功命中的精确关系，优先复用原匹配结果；2）该 case 下的剩余内外账，允许按 `person_name + biz_date + encounter_type` 从当前结果表回捞。
[x] 255. `yb_reconcile_demo` 已新增 [combined_case_detail_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_case_detail_service.py) 与 `GET /api/reconcile/combined/case-detail`：入参支持 `case_key` 或 `person_name + biz_date + encounter_type`，并可附带 `book_run_id / self_pay_run_id`；返回 `meta / book_detail / self_pay_detail`。其中记账侧会按 case 条件查询当前结果表，并对成功类记录额外补抓 `zsh_match_relation / zsh_outer_group_relation`，把命中的内账关系和聚合外账成员一起带回；自付侧会按当前各层结果表回收该 case 下的命中记录与剩余记录。当前边界也已明确：由于结果表仍是覆盖式，历史 `run_id` 的 detail 查询会在 `meta.notes` 中提示“数据库明细以 latest 结果表为准，历史批次只保证快照元信息”，同时前端 API 入口 [frontend/src/api/index.js](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/api/index.js) 也已补出 `getCombinedCaseDetail()` 供后续展开行或抽屉调用。
[x] 256. `yb_reconcile_demo` 已将 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 的展开行改为真实异步明细：当前用户展开某条 case 时，前端会调用 `getCombinedCaseDetail()` 按 `case_key + book_run_id + self_pay_run_id` 拉取 detail，并在展开区域按“记账命中主记录 / 记账命中关联内账 / 记账聚合外账成员 / 记账剩余记录 / 自付命中记录 / 自付剩余记录”六块展示。这样 `/combined` 不再依赖列表接口里硬塞 `book_items / self_pay_items`，而是回到“列表看摘要，展开再看明细”的结构。当前这一步未做前端构建联调，只完成了代码接线与本地状态管理。
[x] 257. `yb_reconcile_demo` 已将 `/combined` 展开区从“工程化六块空表直出”收成“病例摘要 + 非空块展示”：当前 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 会先显示该 case 的 `记账状态 / 记账层级 / 记账内外账笔数 / 金额 / 自付状态` 摘要，再按有无数据决定是否展示“记账外账主记录 / 记账已命中内账 / 记账聚合外账成员 / 记账剩余记录 / 自付命中记录 / 自付剩余记录”。对于 `book_only` 且只有记账内账剩余的 case，页面会明确提示“当前只有记账侧；且没有对应医保/公费外账主记录”，避免再出现一串空表看不出重点的情况。
[x] 258. `yb_reconcile_demo` 已完成 `/combined` 明细区的 `Phase A` 收口：当前 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 已不再用一套固定表头硬套所有记账/自付层，而是改成“按分组自适应字段展示 + 统一显示别名”。具体来说，记账侧会按“主外账 / 已命中内账 / 聚合外账成员 / 剩余记录”分别选择实际有值的列；自付侧会先把 `person_name / PRINTINV_PAT_NAME / fpat_name`、`third_party_time / trade_time / jiao_yi_shi_jian`、`inner_time / LOGLADATE / ffinancial_date`、`third_party_amount / amount / DESC_MONEY` 等异构字段归一成统一显示列，再按层分组渲染。当前即使某组数据没有任何标准别名列，也会回退显示一列 `原始数据`，避免再次出现“有条数但整表全空”的误导性界面。
[x] 259. `yb_reconcile_demo` 已收紧 `/combined` 中“记账侧外账”的展示日期口径：当前 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 在渲染 case detail 时，会读取当前联合结果 `meta.book_date_range`，并只展示落在用户所选日期范围内的公费/医保外账相关记录。也就是说，前后一天仍然保留在匹配逻辑里使用，但不会再在 `/combined` 的“记账外账主记录 / 记账聚合外账成员 / 含外账身份的记账剩余记录”里把 `1 月 31 日 / 3 月 1 日` 这种越界外账直接展示出来；纯内账剩余则不受这条显示规则影响，仍按当前 case 正常展示。
[x] 260. `yb_reconcile_demo` 已在 [plan.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/plan.md) 冻结 `/combined` 的正式业务口径：当前已明确“一个 case = 一个外账侧就医登记号”，而不是 `姓名 + 日期 + 就诊类型` 的临时聚合键；并同步明确顶部 6 个主要数字后续都应按“外账登记号去重后的 case”统计，包括 `联合 case 数 / 双边都成功 / 双边都失败 / 仅记账侧 / 仅自付侧 / 不可联合自付外账` 的正式定义。文档中也已明确记录现状与正式口径的差异：当前页面仍在使用病例级临时聚合，因此像“2 月 8 万多外账却看到 17 万 case”这种现象，本质上是统计主键还没切到登记号口径，不是业务数据真的变成了 17 万个就医 case。
[x] 261. `yb_reconcile_demo` 已进一步冻结 `/combined` 四种联合状态的业务含义：当前 [plan.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/plan.md) 已明确写清，“双边都成功”表示该登记号 case 下既有记账、又有自付；“双边都失败”表示该登记号 case 下既没有记账、也没有自付；“仅记账侧”表示该登记号下只有记账、没有自付，其中“没有自付”特指该外账在自付链上到 `12. 最终剩余档案` 之前仍未被有效挂住；“仅自付侧”表示该登记号下只有自付、没有记账，其中“没有记账”特指该外账在记账链上到 `12. 最终死账` 之前仍未被有效挂住。“不可联合自付外账”也已明确为“剩下的第三方数据”，属于辅助说明数字，不计入 case 本体。
[x] 262. `yb_reconcile_demo` 已对 `/combined` 顶部 6 个主要数字做首轮后端口径收紧：当前 [combined_statistics_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_statistics_service.py) 已不再用“记账病例级 case + 自付病例级 case 并集”直接作为总 case 数，而是改为只以“记账侧存在外账身份的 case”作为 `/combined` 的统计底盘，避免自付侧临时病例聚合把总量从 `7~8 万` 扩张到 `17 万`。同时已给记账快照与自付快照补入 `book_has_effective_match / self_pay_has_effective_match` 布尔量，用来区分“这个 case 是否已真正挂住”，从而把顶部卡片和联合状态分布改成按“有记账 / 有自付”来计算，而不再直接依赖当前 `book_status / self_pay_status` 的高优先级覆盖结果。当前这一步仍属于过渡版：底层 case 主键仍是 `姓名 + 日期 + 就诊类型` 的临时聚合键，尚未彻底切到“外账登记号 case”，但顶部数字已经不会再被纯自付侧病例数无节制放大。
[ ] 263. `yb_reconcile_demo` 待解决：为 `/combined` 正式补“登记号 case”专用底表或展示表，必要时可直接调整现有 SQL / DDL。当前仅靠运行快照里的 `姓名 + 日期 + 就诊类型` 临时聚合键去推断 case，会让顶部数字、列表与 detail 长期处在“过渡口径”。后续建议允许直接改 `lyx_waizhang_detail` 相关 SQL，新增稳定的 `case_id / case_register_no / effective_case_date / case_status_snapshot` 等字段，或者单独落一张 `lyx_combined_case_registry` / `lyx_combined_case_view` 展示表，把“外账登记号去重后的 case 底盘”“该 case 是否已有记账”“该 case 是否已有自付”“展示所需的代表姓名/日期/类型”预先算好。这样可以显著加快 `/combined` 加载速度，也能明显降低后端拼装逻辑复杂度。
[x] 264. `yb_reconcile_demo` 已先冻结本轮“结果表补字段”的结构方案，并明确记录到待办：`zsh_*` 结果表本轮统一补 `run_id`，其中成功主表与外账剩余表统一补 `case_register_no / outer_detail_id`，内账剩余表统一补 `case_register_no / inner_source_pk / inner_business_key / inner_register_no`，两张关系表 `zsh_match_relation / zsh_outer_group_relation` 统一补 `run_id / case_register_no`，并分别补 `outer_detail_id / inner_source_pk / inner_business_key / inner_register_no / group_outer_detail_id / member_outer_detail_id` 等可直接回指源数据的新列。目标是让记账链后续可以不再只靠 `outer_record_id / inner_record_id` 的字符串拼接，而能直接落“批次 + case + 原始明细主键”三层定位。
[x] 265. `yb_reconcile_demo` 已先冻结本轮“自付结果表补字段”的结构方案，并明确记录到待办：全部 `lyt_*` 运行时结果表本轮统一补 `run_id`；第三方侧相关表统一补 `third_party_detail_id`；内账侧相关表统一补 `inner_detail_id / inner_detail_table / inner_business_key`；所有成功表以及允许挂 case 的剩余表统一补 `case_register_no`，并额外预留 `case_link_method` 记录“本条数据是通过登记号直连、发票号回溯还是姓名日期兜底挂上的 case”。这样做的目的不是现在立刻改完逻辑，而是先把所有 lyt 表的物理结构补到位，保证下一轮 Python 落库时不用再二次改表。
[x] 266. `yb_reconcile_demo` 已按上面方案补出 combined 专用 DDL 文件：本轮会新增一份完整建表 SQL，内含 `lyx_combined_case_registry` 与 `lyx_combined_case_relation` 两张表。前者一行一个登记号 case，负责 `/combined` 顶部 6 个主要数字、列表分页和状态摘要；后者一行一条成员关系，负责把记账外账、记账内账、自付内账、第三方流水这些原始明细稳定挂回某个 `case_register_no + run_id`，避免 `/combined/case-detail` 继续依赖 `姓名 + 日期 + 类型` 的临时推断。
[x] 267. `yb_reconcile_demo` 已将“新字段版 zsh 全量重建脚本”纳入本轮实施目标：本轮会直接改 [sql/rebuild_zsh_book_funnel_tables.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/rebuild_zsh_book_funnel_tables.sql)，使其成为可在新电脑直接执行的完整 `zsh_*` 建表脚本，且把 `run_id / case_register_no / outer_detail_id / inner_source_pk` 等新增列一次性写进所有运行时会用到的记账表结构中。你后续只需要重跑这一份 SQL，再重新跑记账结果，就能得到带新字段的 `zsh_*` 表。
[x] 268. `yb_reconcile_demo` 已将“新字段版 lyt 全量重建脚本”纳入本轮实施目标：本轮会把 [sql/rebuild_lyt_self_pay_tables.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/rebuild_lyt_self_pay_tables.sql) 补成一份完整的“所有 lyt 表建表语句”，覆盖 `绿色 / 黄色 / 30 分钟 / 25 小时 / 组合 / 组合2 / 内部冲销 / 特殊 / 最终剩余` 全部自付运行时结果表，并统一纳入 `run_id / case_register_no / third_party_detail_id / inner_detail_id / inner_detail_table / case_link_method` 等预留字段。后续在新电脑上只需执行 `rebuild_zsh_book_funnel_tables.sql + rebuild_lyt_self_pay_tables.sql` 两份文件，就能一次性把当前运行时程序依赖的 `zsh_* / lyt_*` 表完整建出来。
[x] 269. `yb_reconcile_demo` 已开始把新字段真正写进 Python 运行时链路，而不再只停留在 DDL：当前已改 [reconcile_engine.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/reconcile_engine.py)、[book_funnel_store.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/book_funnel_store.py)、[self_pay_matcher.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_matcher.py)、[self_pay_result_store.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_result_store.py)、[third_party_normalizer.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/third_party_normalizer.py)，把 `run_id / outer_detail_id / inner_source_pk / inner_business_key / third_party_detail_id / inner_detail_id / inner_detail_table` 真正带进运行时数据结构和落库 SQL。当前已通过 `py_compile` 语法自检；旧读取链与旧字段仍保留，尚未删兼容逻辑。
[x] 270. `yb_reconcile_demo` 已完成“记账链写入端”的第一阶段收口：1）`OuterRecord / InnerRecord` 已补进 `outer_detail_id / source_pk / business_key`；2）外账加载阶段现在会在表存在 `id` 字段时显式读入，住院/门诊内账也会把 `row_id / SUBINVOICE_ID / fip_serialno / fregister_no` 一起带进运行时对象；3）`_outer_to_json / _inner_to_json / _outer_json_to_fail_row / _inner_json_to_fail_row / _build_funnel_success_row` 已补齐 `run_id / case_register_no / outer_detail_id / inner_source_pk / inner_business_key / inner_register_no / group_outer_detail_id / member_outer_detail_id` 相关字段；4）`book_funnel_store.py` 的 `zsh_*` 插入 SQL 与 `zsh_match_relation / zsh_outer_group_relation` 写库 SQL 已同步升级；5）本轮已开始把 `matched / remain / review / special / final` 基础结果写进 `lyx_book_case_map`。当前剩余边界：`lyx_book_case_map` 仍是“基础映射已落地”，但 `lyx_combined_case_registry / relation` 的正式归集服务还没接。
[x] 271. `yb_reconcile_demo` 已完成“自付链写入端”的第一阶段收口：1）`NormalizedThirdPartyRow` 已补 `detail_id`，标准化第三方读取现在会把 `lyx_dsf_detail.id` 带回运行时；2）门诊/住院绿色、Mix、组合、组合2 的 matched 结果已开始补 `third_party_detail_id / inner_detail_id / inner_detail_table / inner_business_key`；3）`run_self_pay_compare()` 现已在运行开始时生成 `run_id`，并把 `run_id` 注入各层 matched rows 与 `_map_inner / _map_outer / _map_inpatient_inner / _map_zuhe*` 的 fail rows；4）`self_pay_result_store.py` 的绿色层、30m/25h/组合/组合2 通用写库，以及 `special / final` 落库，已开始真正写入新字段；5）本轮已同步把这些层的基础结果写入 `lyx_self_pay_case_map`。当前剩余边界：`case_register_no` 仍严格遵守“能稳定回挂才写”，所以本轮自付映射表里会有一部分记录先保持 `NULL`；内部冲销层 `xiaochu` 也还没有完整接入映射表。
[ ] 279. `yb_reconcile_demo` 待解决：补齐自付 `xiaochu` 层到 `lyx_self_pay_case_map` 的映射写入。当前绿色 / 30m / 25h / 组合 / 组合2 / 特殊 / 最终层已开始产出基础映射，但内部冲销层还没有完整写入 `match_status=matched/remain` 的 map 记录；如果后续 `/combined` 需要把“内部冲销已处理”也当成有效自付侧状态，就需要把这一层继续补全。
[ ] 280. `yb_reconcile_demo` 待解决：实现 `case_register_no` 的稳定回挂服务。当前记账链已经把 `case_register_no` 大量写进 `zsh_*` 与 `lyx_book_case_map`，但自付链因为遵守“严格挂接”，本轮只补了 `run_id + detail_id + business_key`，并未在所有可回挂场景下自动回填 `case_register_no`。后续建议新增独立服务，按 `SUBINVOICE_ID / fip_serialno / 已命中关系 / book_case_map` 去反推自付记录属于哪个外账 case，再回写 `lyx_self_pay_case_map.case_register_no`。
[ ] 272. `yb_reconcile_demo` 待完成：新增“case 映射与归集服务”，不要把 combined 口径直接塞回前端页面里硬算。建议新增独立服务，例如 `combined_case_registry_service.py`，专门负责：1）从 `lyx_waizhang_detail` 按 `group_key_value` 建 case 底盘；2）从 `zsh_* / lyt_*` 按 `run_id + case_register_no` 回填 `book_matched_flag / self_pay_matched_flag / layer_code / 金额 / 笔数`；3）把结果写入 `lyx_combined_case_registry / lyx_combined_case_relation`。这样 `/combined` 顶部 6 个数字、列表、展开详情都不需要再跨一堆结果表现算。
[ ] 273. `yb_reconcile_demo` 待完成：改 `/combined` 的读取端。实施顺序应为：1）[combined_statistics_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_statistics_service.py) 从“读快照临时 case”切换为“优先读 `lyx_combined_case_registry`，没有再回退旧口径”；2）[combined_case_detail_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_case_detail_service.py) 从“按姓名日期类型回捞”切换为“优先按 `run_id + case_register_no` 读 `lyx_combined_case_relation`”；3）前端 [CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 继续沿用现有 UI，但把 case_key 逐步替换成真正的 `case_register_no`。目标是让 `/combined` 从推断式展示，变成登记号主键驱动的展示。
[ ] 274. `yb_reconcile_demo` 待完成：保留双轨兼容，直到新结构验数通过。具体做法应为：1）旧 `latest_run.json / statistics_view / combined_side_cases` 暂不删除；2）新字段落库与 case registry 先并行生成；3）统计接口与 `/combined` 先做“优先新表，没有则回退旧逻辑”；4）待你现场跑数确认“顶部 6 个数字、case 列表、case-detail、book/self-pay 明细笔数”都对，再删旧 `姓名 + 日期 + 类型` 临时聚合链。这一条的目标是把风险控制在“多一条新链”，而不是“直接替换旧链后再追 bug”。
[x] 275. `yb_reconcile_demo` 已确认：自付链 `case_register_no` 的写入边界采用“严格挂接”。也就是只有能通过登记号或可靠业务键稳定反推到外账 case 时才写入，否则留空；不允许仅凭姓名 + 日期之类的弱规则把自付结果硬挂到某个 case。这样做虽然会让部分记录暂时保持 `case_register_no = NULL`，但可以避免 `/combined` 出现假成功、假归属。
[x] 276. `yb_reconcile_demo` 已确认：`run_id` 的持久化策略本轮先保持“数据库只保留 latest，历史批次继续靠 JSON 快照”。也就是 [book_funnel_store.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/book_funnel_store.py) 与 [self_pay_result_store.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_result_store.py) 当前的 `TRUNCATE + 覆盖最新` 策略暂不改；`run_id` 先作为结构字段与 combined 归集字段写入，等新链路验数稳定后再考虑是否升级为数据库内保留历史批次。
[x] 277. `yb_reconcile_demo` 已确认：在现有 `run_id / case_register_no / outer_detail_id / inner_detail_id / third_party_detail_id` 新字段之外，还需要单独落两张映射表来“一劳永逸”固化链路关系，而不是只依赖结果表临时拼装。当前已把这两张表并入 [sql/create_lyx_combined_case_tables.sql](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/sql/create_lyx_combined_case_tables.sql)：`lyx_book_case_map` 用于固化“记账-外账-内账”映射，`lyx_self_pay_case_map` 用于固化“自费-第三方-内账”映射。后续 `/combined` 应优先读“外账 case + 两张映射表 + registry/relation”，而不是继续跨 `zsh_* / lyt_*` 结果表现算。
[x] 278. `yb_reconcile_demo` 已收敛当前阶段的待确认点：本轮后续代码实施默认采用以下统一口径，不再反复拆散确认。1）一个 case 仍等于外账 `group_key_value`；2）门诊与自付没有登记号时，不额外发明新的业务主键，而是优先依赖 `SUBINVOICE_ID / fip_serialno / 各表 id / 已命中关系` 回挂；3）结果表先写结构字段，映射关系最终落到 `lyx_book_case_map / lyx_self_pay_case_map / lyx_combined_case_registry / lyx_combined_case_relation`；4）数据库结果表先保 latest，历史先不入库累积。除非后面你明确改口，这 4 条视为默认方案继续往下做。
[x] 281. `yb_reconcile_demo` 已修复 `/book/funnel` 页面一个前端时序型提示异常：当前 [frontend/src/views/BookFunnelReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/BookFunnelReconcile.vue) 已将页面初始化时自动触发的“已加载最近一次账本漏斗对账结果”成功提示改为静默加载，并新增统一的 `showMessage()` 包装，用 `ElMessage({ grouping: true })` 收口手动提示，避免在页面切换/重复挂载时连续创建多个消息实例，引发 Element Plus 在 `onClose` 阶段的 `ReferenceError: Cannot access 'instance' before initialization`。
[x] 282. `yb_reconcile_demo` 已同步修复 `/self-pay/run` 页面同类 Element Plus 消息时序异常：当前 [frontend/src/views/SelfPayReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/SelfPayReconcile.vue) 已移除挂载时自动弹出的“已加载最近一次自付对账结果”成功提示，并新增统一 `showMessage()` 包装，将“缺少日期提醒 / 执行完成提示”改为 `ElMessage({ grouping: true })`。这样 `/book/funnel` 与 `/self-pay/run` 两个长任务执行页都统一避开了页面切换时重复创建 message 实例的风险点。
[x] 283. `yb_reconcile_demo` 已将 `/combined` 页面的全局说明从正文 `el-alert` 收口为帮助 tip：当前 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 已不再把 `result.meta.notes` 直接平铺在界面顶部，而是改成卡片标题右侧的问号按钮，点击后以和 `/book/funnel` 接近的 `popover + 分条说明` 形式展示“联合看板说明”。这样界面主体只保留数据面板与必要告警，避免诸如“自付侧纯第三方剩余记录缺少 person_name...”这类系统说明长期占据页面正文。
[x] 284. `yb_reconcile_demo` 已将记账漏斗外账入口切换为“优先读取标准化表、失败再降级旧逻辑”：当前 [reconcile_engine.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/reconcile_engine.py) 已新增 `_query_standardized_outer_records()`，`_query_outer_records()` 现在会先读取 `his_inner.lyx_waizhang_detail`，并继续支持按 `allowed_tables_by_db` 基于 `source_db + source_table` 做来源筛选；若标准化表不存在或读取异常，则自动降级回旧的 `lyt_26_2_gf / lyt_26_2_yb_2` 动态扫表逻辑。这样正式链路默认走聚合表，保留 fallback，降低切换风险。
[x] 285. `yb_reconcile_demo` 已将 `book-funnel` 主请求里的 CSV/TXT 导出临时移出执行主链：当前 [reconcile_engine.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/reconcile_engine.py) 在第 11 层完成后的收尾阶段，已不再同步执行各层 `self._write_book_funnel_csv(...)` 与 `self._write_book_funnel_summary(...)`。主请求现在只保留结果落库、统计视图构造与快照写入，并在日志中显式输出“已跳过 CSV/TXT 导出”。后续若还需要完整导出，应单独做成异步任务或手动导出入口，而不是继续阻塞 `/api/reconcile/book-funnel`。
[x] 286. `yb_reconcile_demo` 已将 `book-funnel` 的运行快照改为“轻 latest + 精简 runs”双档写入：当前 [run_registry_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/run_registry_service.py) 已支持 `latest_result` 单独传入，[reconcile_engine.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/reconcile_engine.py) 现在会为 `save_run_snapshot()` 生成两份精简快照。`latest_run.json` 只保留 `/book/funnel` 与统计页必要字段：`meta(run_id/start/end/finished_at/source_filter_enabled/selected_source_keys) + summary + statistics_view + results`；`runs/{run_id}/result.json` 在此基础上继续保留 `combined_side_cases`，供 `/combined` 读取历史批次。与此同时，JSON 写入已改为无缩进紧凑格式，减少体积和磁盘写入耗时；而实时 `POST /api/reconcile/book-funnel` 返回给前端的完整结果对象保持不变，不影响当前页面使用。
[x] 287. `yb_reconcile_demo` 已修复 `self-pay` 住院绿色层一个结果表建表冲突：当前 [self_pay_result_store.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_result_store.py) 在 `_write_inpatient_tables()` 中，若目标表名本身就是模板表（例如 `lyt_zy_zifu_success`），不再执行 `CREATE TABLE IF NOT EXISTS target LIKE target` 这种 MySQL 会报 `1066 Not unique table/alias` 的语句，而是直接进入 `TRUNCATE + INSERT`。同样的兜底也同步加到了 `_write_outpatient_tables()`，避免以后门诊绿色层或其它直接写基础模板表时踩到同类问题。
[x] 288. `yb_reconcile_demo` 已确认并修复自付组合层/组合2层一个 CSV 导出字段不兼容问题：报错 `dict contains fields not in fieldnames: run_id / case_register_no / inner_detail_id / inner_detail_table / inner_business_key / case_link_method` 并不是 `rebuild_lyt_self_pay_tables.sql` 的数据库表缺列，而是 [self_pay_matcher.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_matcher.py) 在写 `zuhe / zuhe2` 的 `inner_unmatched_csv` 时手工指定了旧版 fieldnames。当前 `_write_dict_rows_csv()` 已改为“保留既有列头顺序，并自动追加 rows 里的新增字段”，从而兼容新加的运行时元数据，不再因为 CSV 导出时报错。
[x] 289. `yb_reconcile_demo` 已将 `self-pay` 主请求的收尾策略改成和 `book-funnel` 一致：当前 [self_pay_matcher.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_matcher.py) 已引入 `WRITE_SELF_PAY_ARTIFACTS_IN_MAIN_FLOW = False`，门诊绿色 / 住院绿色 / 黄色 / 30m / 25h / 组合 / 组合2 / 特殊 / 最终层的 CSV/TXT 写入函数在主请求里都会直接跳过，内部冲销层 `xiaochu` 的那段手工 CSV 落盘也只会在显式开启时执行。同时，`save_run_snapshot()` 也已改成“轻 latest + 精简 runs”：`latest_run.json` 只保留 `status + meta + statistics_view + results`，`runs/{run_id}/result.json` 额外保留 `combined_side_cases`，从而减少自付执行完成后的磁盘 I/O 和 JSON 序列化耗时，而不影响当前请求直接返回给前端的完整结果。
[x] 290. `yb_reconcile_demo` 已将 `/combined` 详情区的用户展示列进一步收口为“表格化、去 ID 化”口径：当前 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 已把记账命中外账、记账命中内账、记账聚合外账、记账剩余记录、自付命中/剩余记录里的 `外账ID / 内账ID / 聚合外账ID / 成员外账ID` 等技术字段移出用户表格，只保留姓名、类型、时间、金额、来源、聚合笔数这类用户可读列。这样 `/combined` 展开详情里即使是 `sd_ok｜fail_his / fail_dsf` 等剩余组，也会继续以表格形式展示，但不会把对用户无意义的技术 ID 暴露出来。
[x] 291. `yb_reconcile_demo` 已修复 `/combined` 详情区一个“部分组显示成原始 JSON” 的前端解包问题：记账剩余组 `book_detail.remain_groups` 里的部分行实际是包装对象 `{ row: {...}, matched_inner_rows: [], outer_group_rows: [] }`，而 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 之前直接把外层对象喂给 `normalizeBookRemainRow()`，导致 `display_*` 列全部取空，最终退回到 `raw_json` 列。当前已改为优先解包 `row.row` 后再做展示归一，所以同类剩余组会恢复成正常表格，而不是原始数据块。
[x] 292. `yb_reconcile_demo` 已将 `/combined/case-detail` 里的“自付剩余｜*｜第三方未命中”正式从返回结果中剔除：当前 [app/services/combined_case_detail_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_case_detail_service.py) 已在后端构造 `self_pay_detail.remain_groups` 时直接过滤所有 `category` 包含“第三方未命中”的剩余组，不再把这类无法可靠归属到具体患者/就医 case 的第三方剩余记录写进接口 JSON；同时 [frontend/src/views/CombinedReconcile.vue](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/src/views/CombinedReconcile.vue) 也把旧的单层隐藏规则升级为通用兜底，避免未来其它层把同类组再次展示出来。
[ ] 293. `yb_reconcile_demo` 暂缓启用 `lyx_combined_case_registry / lyx_combined_case_relation` 两张正式 combined 底表。当前这两张表先保留 DDL 与后续设计，不在本轮 `/combined` 收口里强行接入；后续若要彻底替换“按姓名 + 日期 + 类型现查现拼”的临时明细方案，再单独开一轮把列表、顶部数字和 case-detail 统一切到 registry/relation。
[x] 294. `yb_reconcile_demo` 已按“层级优先级最大值”收口 `/combined/case-detail` 的默认展示口径：当前 [app/services/combined_case_detail_service.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/combined_case_detail_service.py) 已为记账侧和自付侧分别补入层优先级表，并在组装 detail 时按“同一明细只保留最高优先级层”的规则进行裁剪。具体来说，记账侧会按外账键/内账键为单位，仅保留该记录出现过的最高层，并把已在 success 层命中的外账、内账从低层 `fail_*` 组中剔除；自付侧会按 `inner_detail_id / inner_business_key / third_party_detail_id` 等稳定键保留最高层，并把已在更高层命中的明细从低层剩余中移除。这样像伍英兰这类“一笔账在多个层里反复出现”的 case，会优先收口成最终 success 或最终 fail，而不再把整条漏斗轨迹全铺出来。
[x] 295. `yb_reconcile_demo` 已补当前一期验收后的文档收口：根目录 [README.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/README.md) 现已新增“当前开发状态 / 文档职责 / 当前仍未收口的开发事项”，明确一期展示已通过、但 combined 正式底表替换、映射回挂、执行中状态恢复等开发事项仍未完成；同时 [frontend/README.md](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/frontend/README.md) 已从默认 Vite 模板改为真实前端交接文档，补齐了页面路由、接口分组、状态恢复、已收口体验问题和 `/combined` 当前定位。后续默认按“根 README 讲系统现状、frontend README 讲前端实现、todolist 讲最近变更与待办”这套方式维护。

============
2026-06-29
============
[x] 296. `yb_reconcile_demo` 暂时关闭“组合对账”作为当前迭代目标：本轮先不继续推进 `/combined` 的正式底表、case registry、case detail 归集和旧口径替换，也不要求新版自付漏斗立即兼容当前 `/combined`。当前 `/combined` 仍保留现有页面和接口，但只作为历史功能与后续参考，避免在新版自付接入阶段被“姓名 + 日期 + 就诊类型”的临时联合口径牵制。
[x] 297. `yb_reconcile_demo` 新增“新版自付漏斗”双轨入口规划：参考记账侧 `/book/run` 与 `/book/funnel` 的关系，旧自付继续保留 `/self-pay/run` 与 `/api/reconcile/self-pay`，新版自付新增 `/self-pay/funnel` 与 `/api/reconcile/self-pay-funnel`。新版跑稳后，再考虑将菜单默认入口切到新版，旧版改为旧版入口或隐藏。
[x] 298. `yb_reconcile_demo` 新版自付后端主线首版已落地：当前已新增 `app/services/self_pay_funnel_engine.py`，并把自付九层算法迁入项目内 `app/services/self_pay_funnel_algorithm.py`，由后端调用 `run_reconcile(start_date_text, end_date_text, csv_dir, output_dir)`。历史个人脚本 `20260625/zf_reconcile_v2.py` 不再作为系统运行依赖。
[x] 299. `yb_reconcile_demo` 新版自付数据源首版已落地：当前仍以甲方外账 CSV 为外账入口，以 HIS MySQL 表为内账入口；门诊链路使用 `p_bhm_paylog / op_chgnote / op_subinvoice`，住院第八层使用 `p_bhm_paylog / IP_PREPAY_DETAIL`。对账最小单位固定为“对账池”，也就是商户订单号对应的整池外账与整池内账，而不是单条记录。
[x] 300. `yb_reconcile_demo` 新版自付结果结构首版已落地：接口返回保持 `meta / summary / statistics_view / results` 的系统风格，`results` 已改为九层漏斗与第十层失败池、第十一层剩余账池。每层分类包含“对账成功池 / 滑向下一层池”；第九层为“差额固定池”；第十层失败池按内账链路断点分为“查询不到内帐 / 仅能查到基本信息 / 金额不一致”三个小 tab；第十一层剩余账池内含 `p_bhm_paylog / op_chgnote / op_subinvoice / IP_PREPAY_DETAIL` 四个小 tab。终端和页面统计都以“对账池”为最小单位，不再输出匹配成功笔数、失败笔数这类行级统计。
[x] 301. `yb_reconcile_demo` 新版自付分页与快照首版已落地：当前已复用 `run_registry_service.py` 的 latest/runs 快照机制，新增 `run_kind = self_pay_funnel`，并新增 `self_pay_funnel_latest_service.py / self_pay_funnel_statistics_service.py`。结果明细分页首版走 JSON 快照，不复用旧 `lyt_*_zifu_*` 表，避免旧表的“第三方流水行级口径”污染新漏斗的“对账池口径”。
[x] 302. `yb_reconcile_demo` 新版自付前端首版已落地：当前已新增 `SelfPayFunnelReconcile.vue`，路由为 `/self-pay/funnel`，菜单在“自付对账”下增加新版“执行界面”，旧 `/self-pay/run` 标注为“执行界面 (旧版)”。页面已支持九层 tab、第十层失败池、第十一层剩余账、remain 四表、对账池明细抽屉、分页和执行中进度轮询。
[x] 303. `yb_reconcile_demo` 新版自付统计接口首版已落地：当前已新增 `/api/reconcile/self-pay-funnel/statistics`，统计指标按对账池设计，包括外账文件行数、对账池数量、总成功池、匹配率、最终剩余池，以及 remain 四表金额为 0 / 不为 0 分布。旧 `/self-pay/statistics` 暂不强行改，后续可增加新版统计入口或在页面上支持选择 run_kind。
[ ] 304. `yb_reconcile_demo` 新版自付 combined 预留规划：虽然本轮先关闭组合对账，但新版自付结果从第一天就应保留 `case_register_no / merchant_order_no / pool_key / outer_count / inner_count / layer_code / status` 等字段。后续恢复 `/combined` 时，优先按外账 case 或商户订单号挂接，而不是回到“姓名 + 日期 + 就诊类型”的临时拼接方式。
[ ] 305. `yb_reconcile_demo` 新版自付实施顺序建议：第一步只做后端服务化和新接口，能返回九层 JSON；第二步接新版执行页和进度条；第三步补 latest/runs/statistics；第四步再考虑结果表与导出；第五步在新版稳定后再讨论 `/combined` 是否恢复以及如何按 case_register_no 统一两边口径。
[x] 306. `yb_reconcile_demo` 新版自付已补齐“每层滑向下一层池”的后端结果口径：当前项目内 `self_pay_funnel_algorithm.py` 的返回结果已包含“第一层滑向下一层”到“第九层滑向最终处理”，`self_pay_funnel_engine.py` 也已将每层结果统一拆成“对账成功池 / 滑向下一层池”两个分类。前端当前为了简化阅读，前九层只直接展示主结果池，不再展示“滑向下一层池”小 tab；第十层失败池和第十一层剩余账仍保留小 tab。
[x] 307. `yb_reconcile_demo` 新版自付算法项目化已完成：已将 `20260625/zf_reconcile_v2.py` 的九层算法复制迁入 `app/services/self_pay_funnel_algorithm.py`，`self_pay_funnel_engine.py` 改为包内导入，不再跨目录 import 项目外脚本。CSV 目录、输出目录、进度回调仍由后端服务显式注入，MySQL 与表名配置继续由 `reference_defaults.py` 注入到算法模块；后续若要进一步“纯服务化”，可再把算法模块中的全局配置拆成参数对象。
[x] 308. `yb_reconcile_demo` 已调整新版自付前端执行体验：`SelfPayFunnelReconcile.vue` 的执行步骤条已从“启动 / 脚本执行 / 一层...七层 / 完成”改为“读取外账 / 查询内账 / 查询VISIT扩展 / 执行对账 / 内账反查 / 写入结果”，并通过项目内 `self_pay_funnel_algorithm.py` 的 `progress_callback` 回调真实阶段；同时 sessionStorage 只保存日期、当前 tab、分类状态和 `run_id`，不再保存完整结果 JSON，避免浏览器报 `Storage quota exceeded`。
[x] 309. `yb_reconcile_demo` 已完成新版自付 JSON 性能优化与页面结构收口：当前主快照 `latest_run.json / result.json` 只保存轻量首页、summary、statistics 和每个分类的 storage 指针，完整明细按分类拆到 `outputs/self_pay_funnel/runs/{run_id}/layers/{layer_code}/{category}.json`；latest 翻页接口会按需读取对应分层 JSON。前端已把“8.剩外账 / 9.剩内账 / 10.失败池”合并为当前第十层“失败池”三个小 tab，把四类 remain 合并为第十一层“剩余账”四个小 tab；执行中区域会显示脚本真实 `print` 输出形成的滚动终端日志。
[x] 310. `yb_reconcile_demo` 新版自付成功池明细展示已优化：前八层“整池对账成功”和第九层“差额固定”的结果，在对账池明细里不再默认展示“剩余外账 / 剩余内账”，也不直接展示完整“匹配记录”大 JSON；页面改为展示投射后的“池信息”，并保留“原始记录”作为兜底排查入口。失败池仍保留匹配记录、剩余外账、剩余内账等排查信息。
[x] 311. `yb_reconcile_demo` 新版自付“池信息”字段投射已按业务阅读顺序重排：详情区固定展示 8 行，依次为 `商户订单号 / 姓名`、`商品名称`、`外账笔数 / 内账笔数`、`csv文件 / csv行号`、`外账交易时间 / 内帐交易时间`、`内帐总金额 / 外账总金额`、`内帐记账金额 / 内帐自付金额`、`内帐明细`。其中外账交易时间来自 CSV 第 1 列，内帐交易时间来自 `op_subinvoice.LOGCDATE`；页面不再展示技术字段 `op_subinvoice.PRINTINV_PAT_NAME` 和 `对账层级`。
[x] 312. `yb_reconcile_demo` 新版自付前九层列表列已收口：前八层成功池和第九层差额固定池列表现在展示 `微信订单号 / 商户订单号 / 时间 / 姓名 / 交易类型 / 交易状态 / 匹配笔数 / 操作`。其中微信订单号来自外账 CSV 第 6 列，时间来自 CSV 第 1 列；交易类型归一为多值支付标签，交易状态按对账池判断为 `付款 / 退款`，只要池内存在退款外账即归为退款。
[x] 313. `yb_reconcile_demo` 新版自付统计卡片已收口：顶部卡片已将九层后失败结果合并为一个“失败池”指标，口径为 `查询不到内帐池数量 + 仅能查到基本信息池数量 + 金额不一致池数量`，并新增“差额固定池”指标展示第九层需复核池数量；失败池指标与页面上的第十层“失败池”三个小 tab 保持一致。
[x] 314. `yb_reconcile_demo` 新版自付排序筛选功能已升级：已实现基于 Element Plus 的表格列头排序 (sortable)，筛选区从本地标签筛选改为服务端筛选，支持 `微信订单号 / 商户订单号 / 患者姓名` 三个搜索框，以及 `交易类型(微信/支付宝/现金)`、`交易状态(付款/退款)` 两个下拉框和重置按钮。搜索范围不再局限当前页，而是由 `/api/reconcile/self-pay-funnel/latest` 读取当前标签对应的完整分层 JSON 后过滤再分页。
[x] 315. `yb_reconcile_demo` 已优化前端终端日志的滚动展示体验：将原本的两个蓝色块文本框移除，改为更紧凑的纯文本间距，并利用 Vue `<transition-group>` 过渡机制实现了上一行向上淡出、下一行向上滑移、新行从下方淡入的终端走马灯滚动动效。
[x] 316. `yb_reconcile_demo` 已实现前端日志缓冲队列播放机制：轮询日志会被缓存并以最小 600ms 的频率平滑播放。当后端执行完成或出错后，前端开启 Fast-Flush 快速冲刷模式（150ms 频率）快进放完剩余日志以保证所有运行内容被用户看见，完成后再自然过渡到结果视图。
[x] 317. `yb_reconcile_demo` 已实现步骤条与播放日志的进度同步：步骤条进度绑定到本地 `displayedActiveStep`。每当弹出的日志中出现阶段前缀时，步骤条高亮才会同步跟进，彻底避免了步骤条超前、以及此前在内账反查阶段因包含“未用于对账”而导致进度回跳/消失的问题。
[x] 318. `yb_reconcile_demo` 已实现页面刷新与载入的控制禁用：刷新进入页面自动获取最近一次结果或者执行长耗时任务时，日期选择器和执行按钮将完全禁用。并为执行按钮添加了 `.is-disabled` 灰色置灰覆盖样式，清除了此前禁用时仍显示高亮蓝色渐变的问题。
[x] 319. `yb_reconcile_demo` 已将新版自付姓名来源从 `op_chgnote.PAT_NAME` 优先切换为 `op_subinvoice.PRINTINV_PAT_NAME`：先通过只读探测确认 `csv -> p_bhm_paylog -> op_chgnote -> op_subinvoice` 跑通记录中 `PRINTINV_PAT_NAME` 非空率为 100%，随后在 `zf_reconcile_v2.py` 的门诊内账 SQL、`InternalRecord` 与 JSON 输出中补入 `op_subinvoice.PRINTINV_PAT_NAME`，并在 `self_pay_funnel_engine.py` 中优先用该字段生成列表和池信息的 `姓名`。
[x] 320. `yb_reconcile_demo` 已调整 `/self-pay/funnel` 自动加载体验：进入页面加载最近一次新版自付结果时显示轻量 loading 标识，并在加载期间禁用日期选择器和执行按钮，避免用户在 latest 结果还未恢复完成时再次触发执行或切换日期。
[x] 321. `yb_reconcile_demo` 已收口新版自付执行进度展示：执行中区域不再展示“大黑终端框”，也不再显示“正在执行新版自付漏斗 / 当前步骤”的静态文本，而是展示脚本真实日志的最近两条业务进度行，例如 `[读取外账] 完成...`、`[查询内账] 批次...`，并自动去掉后端包装的时间戳。
[x] 322. `yb_reconcile_demo` 已精简新版自付前九层页面层级：前九层外层 tab 已足够表达“第一层/第二层/.../差额固定”，页面内不再显示“第一层对账池”等重复标题框，也不再显示“对账成功池 / 滑向下一层池”二级 tab；第十层失败池与第十一层剩余账因本身代表多类结果，继续保留小 tab。
[x] 323. `yb_reconcile_demo` 已补齐新版自付全标签筛选后端链路：`self_pay_funnel_latest_service.py` 新增分层 JSON 全量过滤逻辑，`router.py` 与前端 API 已传递 `trade_type / trade_status / wechat_order_no / merchant_order_no / patient_name` 参数；分页、查询、重置都会基于当前标签重新请求后端，确保列表 `total/page/page_size` 与筛选结果一致。
[x] 324. `yb_reconcile_demo` 新版自付支付方式口径已从单值“交易类型”升级为多值“支付标签”：当前仅从 `PAYMETH_DESC` 的非 0 金额片段提取有效标签，规则为含“记账”归 `记账`、含“微信”归 `微信`、含“支付宝”归 `支付宝`、含“医保”且不含“记账”归 `医保个账`、其它全部归 `其它自费方案`。前端筛选改为多选“支付标签”，一个对账池同时含微信和医保个账时，任一标签筛选都能命中；本轮仍继续使用 JSON + Python 字典，不做 MySQL 迁移。
[x] 325. `yb_reconcile_demo` 新版自付“第十层失败池”已补充内账入口诊断并升级为链路断点分类：当前 `20260625/zf_reconcile_v2.py` 会对九层后失败池的商户订单号批量诊断 `p_bhm_paylog.FGNETTERMINAL -> p_bhm_paylog.FCHGNOTEID -> op_chgnote.CHGNOTE_ID/INVOICE_ID -> op_subinvoice.INVOICE_ID`，并把 `失败分类 / 失败阶段 / 能否查到内帐 / 诊断结论 / paylog/chgnote/subinvoice命中笔数 / FCHGNOTEID / FAMOUNT / FDESC / 链路诊断记录` 写入结果 JSON。前端第十层按“查询不到内帐 / 仅能查到基本信息 / 金额不一致”三个小 tab 展示。
[x] 326. `yb_reconcile_demo` 新版自付第八层住院押金已补“姓名与住院锚点”链路：当前第八层仍按 `p_bhm_paylog.FCHGNOTEID -> IP_PREPAY_DETAIL.FPREPAYDETAIL_ID` 匹配住院押金金额，但查询已扩展为 `IP_PREPAY_DETAIL.FINVOICE_ID -> ip_settlebill_master.FINVOICE_ID -> ip_settlebill_master.FIP_SERIALNO -> ip_inpatient_register.FIP_SERIALNO`，并在 JSON 中带出 `IP_PREPAY_DETAIL.FPAT_NAME / FINVOICE_ID`、`ip_settlebill_master.FIP_SERIALNO / FREGISTER_NO / FINVOICE_ID`、`ip_inpatient_register.FIP_SERIALNO / FPAT_NAME`。后端投射和前端明细也已支持第八层住院押金展示姓名、住院流水号、登记号、发票 ID、押金流水号和押金明细 ID，为后续 `/combined` 按住院锚点关联做准备。
[ ] 327. `yb_reconcile_demo` 组合对账后续思路调整：不要直接把“记账按条记录”和“新版自付按对账池”硬拼，而是先建设统一的 case/anchor 映射层。门诊侧多数情况下记账和自付都落在 `op_subinvoice`，优先用 `SUBINVOICE_ID / INVOICE_ID` 作为共同锚点；住院侧记账主要来自 `ip_settlebill_master`，新版自付第八层来自 `IP_PREPAY_DETAIL`，优先通过 `FINVOICE_ID` 和 `FIP_SERIALNO` 关联，再用 `FREGISTER_NO` 等登记号字段补强。组合对账读取时应按这些锚点聚合“一个 case 下的记账结果 + 自付对账池结果”，而不是退回旧的“姓名 + 日期 + 就诊类型”临时拼接。
[x] 351. `yb_reconcile_demo` 新增 `/combined/new` 作为新版组合对账入口：新版组合已以 `/book/funnel` 新记账漏斗和 `/self-pay/funnel` 新自付漏斗为输入，不直接复用旧 `/combined` 的 `姓名 + 日期 + 就诊类型` 弱拼接口。当前旧 `/combined` 保留不动，新增独立路由和页面用于验数；新版组合的核心已开始转为后端统一 case/anchor 归集，再由页面展示“记账侧状态 + 自付侧状态 + 金额摘要”，而不是前端临时跨结果表拼数据。
[ ] 352. `yb_reconcile_demo` `/combined/new` 批次日期窗口规则：当记账批次与自付批次日期范围不一致时，不做简单 ID 交集，也不能把窗口外数据静默丢掉。实现口径：1）先计算两个批次的日期交集；2）主组合统计和列表只纳入交集日期内的数据；3）若一侧批次有窗口外数据，例如自付比记账多 `2026-02-21 ~ 2026-02-28`，顶部必须提示“窗口外数据未纳入组合统计”，并可提供“窗口外数据”小 tab 展示日期范围、数据量和来源侧；4）若两个批次没有日期交集，则禁止组合并提示“两个批次日期无交集，无法组合对账”。该规则用于避免因日期范围不一致导致假成功、假缺失或静默漏算。
[ ] 353. `yb_reconcile_demo` `/combined/new` 日期字段口径待实现时统一核定：外账 CSV 的业务时间取第一列；`p_bhm_paylog` 取 `FMDATETIME`；`op_chgnote` 取 `SMDATETIME`；`op_subinvoice` 取 `LOGCDATE`；`ip_prepay_detail` 取 `FSMDATETIME`（若现有新版自付链路继续使用 `FFINANCIAL_DATE`，则以当前项目实际字段为准并在说明书里写明）；住院记账 `ip_settlebill_master` 的时间字段按当前项目现有实现为准，优先复核 `FSETTLE_DATE / FSMDATETIME` 哪个已被 `book/funnel` 标准化结果采用。原则：组合窗口判断使用各结果记录已经落地的业务日期字段，只有缺失时才回退到上游原表时间字段。
[ ] 354. `yb_reconcile_demo` `/combined/new` 页面交互口径：新版组合对账不是重新执行对账的页面，而是读取已完成的记账批次与自付批次，自动计算两个批次日期范围的交集，并默认按该交集展示联合看板。页面允许用户在交集窗口内进一步筛选某一天、某几天或某一周；只要用户选择的期望时间段整体落在交集内，就可以作为主组合统计和列表筛选条件。如果用户选择的时间段超出交集，页面应禁止纳入主组合统计，并提示“该时间段超出可组合窗口，只能查看窗口外单侧数据/提示，不能计入组合结果”。该页面应明确区分“组合统计窗口”和“窗口外单侧数据提示”。
[x] 355. `yb_reconcile_demo` 后续 run_id / runs 文件夹命名已改为优先体现对账日期范围：`run_registry_service.build_run_id()` 已支持 `start_date/end_date`，新生成的 `book_funnel`、旧 `self_pay`、新版 `self_pay_funnel` 批次会采用类似 `book_funnel_2026-02-01_to_2026-02-20`、`self_pay_funnel_2026-02-01_to_2026-02-28` 的命名；只有未传日期范围的旧调用才继续使用执行时间戳。这样后续 `outputs/*/runs` 目录更接近业务批次，而不是只反映执行时间。
[x] 356. `yb_reconcile_demo` `/combined/new` 第一阶段批次窗口骨架已完成：已新增后端服务 `combined_new_service.py` 和接口 `GET /api/reconcile/combined-new`，读取 `outputs/book_funnel` 与 `outputs/self_pay_funnel` 的运行快照，返回两侧批次元信息、日期范围、日期交集、用户筛选日期是否合法、窗口外单侧数据提示。前端已新增 `/combined/new` 页面和菜单入口，支持选择记账批次、自付漏斗批次、交集内日期筛选。
[x] 357. `yb_reconcile_demo` `/combined/new` 第二阶段 anchor 归集首版已完成：后端已在 `combined_new_service.py` 中接入真实归集逻辑，记账侧优先读取 `lyx_book_case_map`，自付侧优先读取 `lyx_self_pay_case_map`；由于新版自付漏斗当前尚未写入该映射表，现阶段会从 `outputs/self_pay_funnel/runs/{run_id}/layers` 分层 JSON 抽取 `op_subinvoice.SUBINVOICE_ID / INVOICE_ID`、住院 `FINVOICE_ID / FREGISTER_NO / FIP_SERIALNO` 等 anchor，并生成 `combined_anchor_cache.json` 作为过渡缓存。接口新增 `page/page_size` 分页参数，前端“组合归集结果”已从占位提示改为真实表格，展示 anchor、姓名、日期、组合状态、记账侧记录数/层级/case、自付侧记录数/层级/商户订单和金额摘要。
[x] 358. `yb_reconcile_demo` 新版自付漏斗已正式写入 `lyx_self_pay_case_map`：当前 `/self-pay/funnel` 在整理九层 JSON 结果时，会同步抽取门诊 `op_subinvoice.INVOICE_ID / SUBINVOICE_ID` 与住院 `FINVOICE_ID / FREGISTER_NO / FIP_SERIALNO / FPREPAYDETAIL_ID` 等 anchor，生成 `run_id / match_group_no / layer_code / match_status / inner_business_key / person_name / biz_date / inner_amount / third_party_amount / third_party_time / inner_time` 等字段，并通过新增 `self_pay_funnel_case_map_store.py` 以 `DELETE WHERE run_id = ? + INSERT` 方式写入 `lyx_self_pay_case_map`。后续新批次 `/combined/new` 会优先从该表读取；历史批次若未写表，仍可降级读取分层 JSON 缓存。
[ ] 359. `yb_reconcile_demo` `/combined/new` 后续应补历史自付批次 anchor 回填工具：第 358 只保证新执行的 `/self-pay/funnel` 会写 `lyx_self_pay_case_map`，已经存在的历史 `outputs/self_pay_funnel/runs/*` 仍可能没有映射表数据。目前组合页会降级读 JSON 缓存；如果要提高历史批次加载速度，可单独做一个“按 run_id 从分层 JSON 回填 lyx_self_pay_case_map”的后台脚本或管理按钮。
[x] 360. `yb_reconcile_demo` 新版自付漏斗内账配置已迁入项目配置并切到 `his_inner`：当前 [app/core/reference_defaults.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/core/reference_defaults.py) 已新增 `SELF_PAY_FUNNEL_DB / SELF_PAY_PAYLOG_TABLE / SELF_PAY_CHGNOTE_TABLE / SELF_PAY_SUBINVOICE_TABLE / SELF_PAY_IP_PREPAY_DETAIL_TABLE / SELF_PAY_IP_SETTLEBILL_MASTER_TABLE / SELF_PAY_IP_INPATIENT_REGISTER_TABLE`，统一指向迁移后的 `his_inner` 与同名表。后端 [self_pay_funnel_engine.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_funnel_engine.py) 在调用项目内 [self_pay_funnel_algorithm.py](/c:/Users/afrangry/PycharmProjects/tp/yb_reconcile_demo/app/services/self_pay_funnel_algorithm.py) 前会覆盖算法模块中的 MySQL 和表名配置，避免系统运行依赖个人脚本默认值；个人脚本 `20260625/zf_reconcile_v2.py` 仅保留为实验备份，不再是系统部署依赖。
[ ] 328. `20260625/zf_reconcile_v2.py` 第五层固定差额待做脚本侧探测：先不改系统匹配口径，而是在脚本里增加诊断/导出能力，专门筛出“外账对账金额 - 内账 `op_subinvoice.SUM_TOTAL` 属于 `12/13/21/26/35`，且外账 `医保其他金额` 能解释该差额，但同 `VISIT_ID` 下不存在另一条 `op_chgnote.SUM_TOTAL = 差额` 或绝对值相等记录”的账。目标是先找到这类账的商户订单号、外账行号、当前 `op_chgnote.VISIT_ID`、差额、医保其他金额、同 VISIT 扩展记录列表，再判断第五层是否需要从“医保其他金额可解释即可对上”收紧为“必须同时存在同 VISIT 差额证据”。
[x] 329. `20260625/zf_reconcile_v2.py` 第五层已收紧为“固定差额必须有同 VISIT 内账证据”：当前第五层不再把“外账医保其他金额 = 固定差额”单独视为成功证据。实现口径：1）仍先计算 `total_diff = 外账对账金额 - op_subinvoice.SUM_TOTAL`，要求绝对值属于 `12/13/21/26/35`；2）无论外账 `医保其他金额` 是否为空，只要进入固定差额逻辑，都必须通过 `internal.chgnote_visit_id` 查询 `VISIT_CHGNOTE_INDEX`；3）候选 `op_chgnote` 不能是当前 `CHGNOTE_ID`，且 `SUM_TOTAL` 需等于 `total_diff` 或绝对值等于 `abs(total_diff)`；4）候选记录优先通过同 `VISIT_ID` 本身作为同人强约束；5）候选记录时间要求早于当前内账时间；6）外账 `医保其他金额` 若有值，仍可作为外账侧拆分校验，要求它与 `total_diff` 同向等值，但不能替代内账证据；7）医保个账、医保统筹、医保自费资金等原第五层拆分校验继续保留。类似 `w004962026020301305` 这种只有外账医保其他金额、但找不到同 VISIT 差额内账证据的池，会从第五层滑出。
[x] 330. `20260625/zf_reconcile_v2.py` 已新增“第六层：固定差额缺证据但正负冲销净额平衡”：原先讨论中的“5.5 层”不使用小数命名，正式作为新第六层；原第六层“医院优惠”已顺延为第七层，原第七层“住院押金”已顺延为第八层。该新第六层用于处理“单笔内外账不平、且找不到同 VISIT 差额内账证据，但同一对账池下付款与退款整体冲销后，外账合计为 0、内账合计也为 0”的账。当前实现为池级 matcher：第五层未通过后，要求当前池内外账同时存在付款和退款、每笔外账与一笔门诊内账形成固定差额结构、外账对账金额合计为 0、内账 `op_subinvoice.SUM_TOTAL` 合计为 0；通过后标记为“净额无风险，明细需复核”。实现不会从对账窗口外补外账，也不会把同 VISIT 下其他无关账自动纳入净额合计。
[ ] 331. `yb_reconcile_demo` 新版自付漏斗后续需按“证据强度”复盘层级顺序：当前层级总体可用，但后续讨论应从证据强度出发，而不是只按历史添加顺序。初步理解：第一层 `PAYMETH_DESC单项金额` 是最直接证据；第三层 `纯自费主金额字段回退` 约束较严、风险较低；第四层 `当前池内组合金额` 属于池内证据完整；第五层收紧后属于“固定差额 + 同 VISIT 额外内账证据”；新增第六层属于“净额平衡但明细缺证据”的风险兜底；医院优惠层有 `PAYMETH_DESC.医院优惠` 直接证据，后续可评估是否应早于固定差额层；住院押金是 IP 表域兜底，应继续放在门诊 OP 层之后。第二层是最需要重构的历史层，后续不建议简单移动，而应改造成基于 PAYMETH_DESC 数据字典的医保拆分精确匹配。
[x] 332. `yb_reconcile_demo` “数据上传界面”旧开放项已收口为新需求：不再把它附着在“记账对账 / 自付对账 / 组合对账 / 智能图表 / 医保日对账”任一既有页面里，而是新增一个独立菜单/页面“外账上传”。页面入口先支持用户选择“记账 / 自付”两类外账；记账上传先保留壳子不实现，当前优先实现自付上传。自付上传的落点先记录为 `yb_reconcile_demo/input/自费`。上传文件类型、批次管理、覆盖/追加策略、校验反馈、与 run_id 的绑定关系等细节暂不展开，等后续单独讨论。
[ ] 333. `20260625/zf_reconcile_v2.py` 第五层固定差额证据占用机制已完成第一步，仍待补 subinvoice 联动占用：当前已维护运行期 `USED_VISIT_EVIDENCE_CHGNOTE_IDS`，第五层候选差额证据会排除已使用 `op_chgnote.CHGNOTE_ID`；第五层成功后，该证据会立刻标记为已使用，remain 反查里也会把该 `op_chgnote` 视为已用于对账。未完成部分：后续需要通过差额 `op_chgnote.INVOICE_ID -> op_subinvoice.INVOICE_ID` 找到对应 subinvoice，并同步占用对应 `SUBINVOICE_ID`，这样 remain_op_subinvoice 才能完整体现“1 外账 : 多内账”的已用证据。
[ ] 334. `20260625/zf_reconcile_v2.py` 第五层展示形态待升级为“1 笔外账对应多笔内账”：当外账金额与当前 `op_subinvoice.SUM_TOTAL` 差固定金额，且同 `VISIT_ID` 找到差额 `op_chgnote` 证据时，展示时不能只展示当前那一笔 `op_subinvoice`。应把“当前主内账 + 差额证据内账”一起写入匹配记录，最好继续用差额 `op_chgnote.INVOICE_ID` 关联 `op_subinvoice`，展示两笔内账的 `INVOICE_ID / SUBINVOICE_ID / SUM_TOTAL / SUM_PAYMENT / SUM_RECEIVABLE / PAYMETH_DESC / 时间`，让业务人员能看出这是 `1 外账 : 2 内账` 的配平。
[x] 335. `20260625/zf_reconcile_v2.py` 第六层净额平衡规则已细化为池级 matcher：当前新增 `classify_pool_layer6_net_zero(...)`，不再沿用纯 `build_edge_layerX(external, internal)` 的单边成功判定。实现条件：1）第五层未通过；2）当前对账池内外账至少存在付款/退款冲销形态；3）只使用当前对账窗口内实际读入的外账，不从窗口外补外账；4）外账 `对账金额` 合计为 0；5）候选内账通过当前池商户订单号直接关联到的 `p_bhm_paylog -> op_chgnote -> op_subinvoice` 记录合计也为 0；6）优先要求 `op_subinvoice.SUM_TOTAL` 合计为 0，并在字段校验中展示 `SUM_PAYMENT`、`SUM_RECEIVABLE` 合计；7）通过后状态标记为“净额无风险，明细需复核”，并保留正负两边的外账和内账明细。
[ ] 336. `20260625/zf_reconcile_v2.py` 第六层 VISIT 回查边界待明确：`VISIT_ID` 回查仍然保留，但只能作为“候选证据池”，不能把同 VISIT 下所有 `op_chgnote` 无脑纳入合计。像 `w004962026020301305` 中同 `VISIT_ID` 查出第三笔更晚、外账中不存在的账，这笔不能干扰正负冲销判断。实现时应优先使用当前商户订单号通过 `p_bhm_paylog.FCHGNOTEID` 直接连到的两笔内账；若需要同 VISIT 扩展，也必须找“金额刚好组成净额 0 的最小组合”，排除已占用内账，并在 JSON 中明确哪些 VISIT 扩展记录被采用、哪些只是候选未采用。
[x] 337. `20260625/zf_reconcile_v2.py` 第六层跨日与对账窗口规则已固化：第六层允许正负冲销跨自然日，例如 2 月 3 日付款、2 月 4 日退款，只要两边外账都在本次 `RECONCILE_START_DATE ~ RECONCILE_END_DATE` 对账窗口内即可；但如果本次只对 2 月 4 日，外账侧缺少 2 月 3 日付款，则不能因为内账 VISIT 回查能看到 2 月 3 日付款而判定配平。当前脚本只使用当前池内外账进行合计，并在 JSON 字段校验中标记“是否使用VISIT扩展补入窗口外外账=False”；该规则也已写入 `20260625/对账说明书.txt`。
[x] 338. `yb_reconcile_demo` 新版自付层级重命名与前后端联动已完成：新增第六层后，原第六层“医院优惠”已改为第七层，原第七层“住院押金”已改为第八层。已同步修改 `zf_reconcile_v2.py` 的层名、summary key、result JSON 分类名、`self_pay_funnel_engine.py` 的分类映射、`SelfPayFunnelReconcile.vue` 的 tab 文案和统计卡片，以及 `20260625/对账说明书.txt`。本轮继续新增第九层“差额固定”，失败池顺延为第 10 个 tab，剩余账顺延为第 11 个 tab。
[x] 339. `yb_reconcile_demo` 新版自付已新增第九层“差额固定”：第八层住院押金之后、最终失败池之前，新增专门收留固定差额池的层级。实现口径为：前八层未通过后，若同一商户订单号池内外账与门诊内账能够整池形成 `12/13/21/26/35` 等固定差额结构，则状态标记为 `fixed_diff`，进入第九层“差额固定池”。该层计入总对账成功池和匹配率，但仍保留“差额固定，需复核”的风险定位；匹配到的内账也纳入已使用 ID，避免 remain 中重复出现。
[x] 341. `yb_reconcile_demo` 新版自付对账率口径已调整为前九层：`20260625/zf_reconcile_v2.py` 的 `总对账成功池数量` 和 `按对账池计算的匹配率` 已把第九层“差额固定池”纳入分子；前端统计卡片说明改为“前九层累计成功池数量”，`20260625/对账说明书.txt` 也同步明确第九层计入匹配率但仍需复核。
[x] 340. `yb_reconcile_demo` 新版自付执行步骤条已在末尾增加“写入结果”：脚本 `20260625/zf_reconcile_v2.py` 在写出 JSON 前输出 `[写入结果]` 日志并调用 `progress_callback("写入结果", 5)`；前端 `SelfPayFunnelReconcile.vue` 步骤条新增“写入结果”，并根据真实日志推进到最后一步。
[x] 342. `yb_reconcile_demo` 新版自付第十层失败池已改为按“内账链路断点”分类：脚本会对九层后仍失败的池批量诊断 `p_bhm_paylog -> op_chgnote -> op_subinvoice` 链路，并输出三个小 tab：`查询不到内帐`、`仅能查到基本信息`、`金额不一致`。`查询不到内帐` 表示商户订单号无法查到 paylog/FCHGNOTEID；`仅能查到基本信息` 表示止步于 paylog 或 chgnote/invoice，尚未到达可用 subinvoice；`金额不一致` 表示已经到达 op_subinvoice 但金额结构未通过前九层规则。前端第十层表格列和明细抽屉已按这三类重新设计，明细暂按“链路诊断 + 外账摘要 + 已查到的内账摘要 + 原始记录”展示，后续可继续按业务阅读习惯调整。
[x] 343. `yb_reconcile_demo` 第十层失败池前端展示已按业务名二次收口：`仅能查到基本信息` tab 不再展示技术列 `p_bhm_paylog.FAMOUNT / FDESC / FCHGNOTEID`，改为 `内帐金额 / 支付渠道`，并删除 FCHGNOTEID 列；该类明细中删除 `op_chgnote命中笔数 / op_subinvoice命中笔数 / 内帐交易时间 / 内帐总金额 / 内帐明细 / 匹配记录 / 剩余内账`，`p_bhm_paylog命中笔数` 改为“查到相关内帐数量”，`FAMOUNT` 改为“内帐金额列表”并单独占一整行。`金额不一致` tab 明细删除“失败阶段/能否查到内帐”和 paylog/chgnote/subinvoice 命中笔数，保留外账交易时间/金额与内帐交易时间/金额上下对照，`FDESC` 改为“支付渠道”，并删除“匹配记录”。
[x] 344. `yb_reconcile_demo` 新增独立“外账上传”页面已完成第一版：当前已新增 `/outer-upload` 独立路由和左侧菜单入口，页面独立于“记账对账 / 自付对账 / 组合对账 / 智能图表 / 医保日对账”。页面顶部支持选择上传类型 `自付 / 记账`；`记账` 先做占位壳子并提示暂未实现；`自付` 支持多文件上传，并最终保存到 `yb_reconcile_demo/input/自费`。上传文件命名允许同一天多份，例如 `2026-02-01 (2).csv`、`2026-02-01 (3).csv`；上传后是否直接触发对账、是否绑定批次、是否做覆盖策略，本轮仍不展开。
[x] 347. `yb_reconcile_demo` 外账上传页面的自付上传区已完成第一版：左侧已实现“拖拽文件到此处 或 点击上传”的多文件 CSV 上传框；选择文件后不会立刻落盘，而是在页面下方预览待上传文件前 10 行。若选择多个文件，下方预览区用文件名 tab 切换，tab 名保留原文件名。预览区底部提供“取消”和“确认上传”两个按钮；取消会清空待上传文件，确认上传才调用后端保存到 `input/自费`。
[x] 348. `yb_reconcile_demo` 外账上传页面的自付上传校验已完成第一版：后端新增 `outer_upload_service.py`，复用新版自付漏斗的 CSV 编码候选和“表头优先 + 固定列兜底”口径。当前阻断上传的必要字段为交易时间、微信订单号、商户订单号、交易类型、交易状态、总金额、退款金额、商品名称、自费金额等基础字段；医保支付资金、医保自费资金、医保个账金额、医保统筹金额、医保其他金额改为可选字段，因为 `2026-02-26 (1).csv` 这类补充文件没有这些字段。校验失败时不写入文件，并在预览区显示缺失字段与对应文件名；可选字段缺失只展示黄色提示。
[x] 349. `yb_reconcile_demo` 外账上传页面的上传日历已完成第一版：右侧“上传日历”会扫描 `input/自费` 中已有 CSV，按文件名日期聚合已上传天数和文件列表。每个已上传日期都是按钮，点击后在页面下方预览该日期对应 CSV 文件的前 10 行；如果同一天有多份 CSV，下方预览用文件名 tab 切换。按本轮确认，下载功能先不做；由于点击日期已经能预览，悬停区不再保留“预览”小眼睛，只保留删除操作。
[x] 350. `yb_reconcile_demo` 外账上传页面的批量删除已完成第一版：上传日历右上角提供“批量删除”和“刷新”按钮。点击批量删除后进入选择模式，已上传日期变成灰色待选状态；点击某天后变红表示已选。选择模式下提供“全选本月”三态复选框：空表示本月全不选，勾表示本月已上传日期全选，横线表示只选中了本月部分日期；同时提供“删除已选 N 天”和“退出”按钮。删除动作只删除 `input/自费` 中对应日期的自付外账 CSV，不影响记账目录。
[x] 345. `yb_reconcile_demo` 第十层“金额不一致”池信息已升级为整池金额结构视角：表格删除 `交易状态 / 剩余外账笔数 / 剩余内账笔数`，`内帐总金额` 改为 `内帐金额合计`；明细删除 `诊断结论`，在 `csv文件 / csv行号` 下方新增 `内帐记录数 / 外账记录数`，并在 `支付渠道` 上方新增 `内帐金额明细 / 内帐金额渠道`。脚本诊断会按去重后的 `op_subinvoice` 记录汇总所有 `SUM_TOTAL` 与 `PAYMETH_DESC`，让“一笔外账 +26，但内账 +26/-26 冲销”这类问题能直接在池信息中看出来。
[x] 346. `yb_reconcile_demo` 新版自付 CSV 默认输入目录已切换到项目内相对路径 `input/自费`：`app/services/self_pay_funnel_engine.py` 的 `DEFAULT_CSV_DIR` 已从旧实验目录 `20260625` 改为 `yb_reconcile_demo/input/自费`，运行结果 `meta.csv_dir` 也记录实际使用目录；`20260625/zf_reconcile_v2.py` 直接运行时同样优先读取 `yb_reconcile_demo/input/自费`，目录不存在时才回退脚本所在目录。`.gitignore` 已保留 `input/自费/*`，避免上传的外账 CSV 进仓库。
